# R38-WikiBridge-A = POSITIVE (passes your paraphrase gate): passage text → internalized closed-book QA in weights

Files: `s3/wikibridge.py`, `docs/cloud_results/r38_squad_para.{log,json,manifest}`. Pushed 3b34c19
(FINDINGS written). Paraphrase eval added exactly as you specified (train on orig question, eval on frozen
Qwen2.5-3B-Instruct paraphrase; base-hard + RAG-answerable on the paraphrase too).

## Result (3 streams × 5 SQuAD articles × 5 QA = 75, base O/P EM 0.000/0.000, RAG-gold 0.973/0.973)

| arm | orig-EM | **para-EM (internalization)** | old-para (t2) |
|---|---|---|---|
| base_no_ingest | 0.000 | 0.000 | — |
| rag_gold_passage | 0.973 | 0.973 | — |
| naive_cpt | 0.000 | 0.000 | 0.0 |
| compact_cpt_only | 0.013 | 0.000 | — |
| **compact_cpt_qa** | **1.000** | **0.893** | **0.90** |

Against your pass bar: `compact_cpt_qa.para_em 0.893` ≥ base 0.000 +0.25 ✓ (by +0.89); ≥ naive 0.000 +0.20
✓; old-stream para retention 0.88–0.90, drop from post-commit (0.92→0.88) ≤ 0.15 ✓; RAG (0.973) reported as
upper bound only ✓; closed-book, no inference memory ✓. **compact_cpt_qa recovers 92% of RAG's answer
quality from weights alone on held-out reworded questions.**

Your paraphrase gate landed us in your top row: **cpt_qa high on original AND paraphrase, old paraphrase
retained** → "S1 QA teacher is a credible bridge from text to reusable in-weight knowledge." The
memorization gap (orig 1.0 → para 0.893, ~0.11) is small — mostly real internalization. And your predicted
decisive split held at scale: **naive_cpt / compact_cpt_only ≈ 0** (raw next-token reading does not
internalize) vs **compact_cpt_qa 0.893** (QA/answer-function targets are load-bearing). Two bugs the smoke
caught: fp16 AdamW → NaN (switched to **bf16**); `\n` stop token for clean decode.

## Honest caveats (want your read on which matter for the claim)

1. **Scale**: 0.5B, only **15 articles / 3 streams / 1 seed** survived the strict para-screen (base must
   fail the *paraphrase* AND RAG must answer it). Signal is clean but small.
2. **Answer type**: extractive short spans only; no free-form / multi-hop.
3. **cf audit RUNNING now** (`WB_SOURCE=cf`, base-ignorance-guaranteed edited-number facts) per your
   "mandatory audit" — to confirm the win isn't SQuAD base-leakage. Will report.

## Next-step options (your ranking?)

Given a clean positive, I see four directions — which order?
1. **cf audit** (running) — confirm base-ignorance. Blocking for the strong claim per your earlier note.
2. **Scale**: more articles/streams/seeds (loosen para-screen or over-select harder) for robustness +
   variance; maybe 1.5B for the scale-trend question.
3. **Footprint sweep** (R36-A-style): cap old committed-QA replay to K/article, report replayed vs
   non-replayed OLD paraphrase retention — does the O(#facts) bound from synthetic reproduce on real text,
   or does real-text redundancy let sub-linear coverage generalize (your Q1)? This is the most
   scientifically load-bearing follow-up IMO.
4. **Multi-view teacher** (`compact_cpt_qa_multiview`): if we want to close the 0.11 memorization gap /
   push paraphrase higher, store a few QA views per fact at commit.

My lean: cf audit (running) → **footprint sweep (3)** as the key science (it directly tests whether the
real-text redundancy softens the R36-A independent-fact O(#facts) bound — the crux of whether this scales
to "hundreds of books" cheaply) → then scale. Do you agree the footprint sweep is the highest-value next
probe, or would you scale first?

(Separately: R37-A clean run overturned the oracle "paradox" — it was your base-KL confound. Clean 2-seed:
grow_local_decoy 0.608, oracle 0.835 (vs naive 0.387, nswrite 0.673, replay 0.875). And clean nogrow-decoy
seed-0 = 0.300 << grown 0.608 → localized-write is the FIRST growth-necessary mechanism in the arc. Writing
R37-A FINDINGS once nogrow seed-1 lands.)
