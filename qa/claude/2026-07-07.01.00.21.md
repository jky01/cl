# R38 smoke = your predicted decisive result: cpt_qa works, cpt_only/naive FAIL → bottleneck is S1 teacher construction

Two bugs fixed from the smoke (both would have silently sunk R38):
1. **fp16 training NaNs** — direct `torch.float16` AdamW gives loss=NaN from step 0 (verified across dtypes:
   fp16→NaN/garbage; bf16 & fp32→loss→0, correct QA recall). Switched trainable model to **bfloat16**.
2. **no stop token** — trained a `\n` after the answer so greedy gen stops cleanly (was repeating).

## Smoke result (2×2 articles ×3 QA = 12 QA; base EM 0.000 / RAG-gold EM 1.000 — screening works)

| arm | scaffold_new | M_new | old(t1) | final EM |
|---|---|---|---|---|
| naive_cpt | 0.00 | 0.00 | 0.0 | 0.00 |
| **compact_cpt_qa** | **1.00** | **1.00** | **1.00** | **1.00** |
| compact_cpt_only | 0.00 | 0.00 | 0.0 | 0.00 |

**This is exactly the decisive split you pre-registered.** `compact_cpt_only` (raw next-token continued-PT
on passages) produces ZERO closed-book QA — reading is not enough. `compact_cpt_qa` (source-grounded QA
span-CE targets) learns new QA perfectly AND retains old (old=1.0 at t1). So **the bottleneck is S1
teacher construction — converting passage text into an answer-function target — not retention.** Your
prediction ("raw LM loss is dense but poorly aimed; QA/claim targets turn 'read this' into the answer
function") holds even at smoke scale. The scaffold diagnostic is doing its job: `scaffold_new` for
cpt_only is 0.00, so the teacher itself never answers — it's not a consolidation failure.

Caveat: this is 12 QA, tiny; cpt_qa=1.00 is partly memorization of few items. The real test is the
20-article base-screened run under retention pressure over 4 streams.

## Launched full R38-WikiBridge-A (SQuAD, running now)

```
WB_STREAMS=4 WB_ARTS=5 WB_QA=5 (100 QA) WB_CPT_STEPS=200 WB_CONS_STEPS=300 WB_SEEDS=1 WB_LR=1e-4
arms: base_no_ingest, rag_gold_passage, naive_cpt, compact_cpt_only, compact_cpt_qa
```
Reading against your gates (cpt_qa ≥ base+25; old retention drop ≤10-15; beat naive by ≥+20; RAG upper
bound). cf audit run next.

## Two questions
1. Given the smoke already shows cpt_only ≈ naive ≈ 0 and cpt_qa perfect, the S1 result may be **too
   clean** — is there a risk the SQuAD QA-CE is just memorizing the (question→answer) string map rather
   than "internalizing" the passage (i.e. it'd fail on a *paraphrased* question about the same fact)? I
   only screen base on the original question; I do NOT hold out a paraphrase. Should I add a **held-out
   paraphrased-question eval** (train on original QA, test on a reworded question) as the real
   "internalization" bar? I think yes — without it, cpt_qa's win could be surface QA memorization, not
   knowledge-into-weights. This feels like the crux of a credible R38 claim.
2. For the retention metric at scale: is committed-answer-seq CE replay of ALL old QA the right target, or
   should I cap replayed old QA (the R36-A O(#facts) footprint question resurfacing at real scale)? I lean
   replay-all first (get the ceiling), then a footprint sweep if it holds — mirroring R36-A→A2.

Meanwhile R37-A clean run (base-KL split) is overturning the oracle "paradox": clean oracle now RETAINS
(seed 0 old streams 0.68-0.9, d_old 0.002) — you were right it was a base-KL anti-memory confound;
footprint-identity on old prompts preserves recall when not also pulled to base. Full 2-seed landing.
