# Structural continual result lands on your strongest cell: joint-oracle SUCCEEDS + consolidation SUCCEEDS → growth is NOT necessary; replay-free shared-weight consolidation retains arith (4x-extrap) AND acquires quad. Growth (frozen trunk + gated adapter) retains exactly but FAILED to even acquire quad.

File `s4/recur_struct.py`, log `docs/cloud_results/lm_recur_struct.txt`. arith(order-1) → quad(order-2)
over F_23, single-token, prefix-only, analytic order gate (2nd-diff==0?). p=23, r=16 adapter, H=12,
eval free-running exact at H/2H/4H, disjoint seen/held delta sets.

```
                  arithA retain (H/2H/4H)   quadB acquire       held (both)
after A           1.00/1.00/1.00            0/0/0              0
joint A|B ORACLE  1.00/1.00/1.00            1.00/1.00/1.00     0
fixed A->B naive  0.00/0.00/0.00            1.00/1.00/1.00     0
consolidation     1.00/1.00/1.00            1.00/1.00/1.00     0
grown+gate        1.00/1.00/1.00            0.00/0.00/0.00     0   (adapter 25.4K, arith max|Δ|=0.00e+00)
```

## Mapping to your outcome table

This is the first cell: **fixed joint oracle succeeds AND consolidation succeeds ⇒ growth is not
necessary; shared-weight continual integration is the stronger result.**

1. **Capacity is sufficient.** The joint A∪B oracle holds both behaviors at 1.00 to 4xH. So the sequential
   failure is interference/optimization, not representational insufficiency — exactly the distinction you
   insisted on before any necessity claim.
2. **Fixed-capacity sequential fails** (naive erases arith), reproduced on the structural task, not just
   the arbitrary delta partition.
3. **Replay-free consolidation SOLVES it in shared weights**: pseudo-rehearsal (distill the frozen arith
   teacher on generated arith prefixes) retains arith at all horizons (the EXTRAPOLATING behavior, 4xH)
   AND acquires quad, no raw-A replay, no task ID, memory-free at deployment. This is the knowledge-into-
   shared-weights positive.
4. **Growth is not necessary and here underperforms**: the frozen-arith-trunk + gated r=16 adapter gives
   EXACT arith retention (max|Δ|=0) — the analytic order gate works perfectly — but the adapter could not
   acquire the order-2 quad recurrence at all (0.00). So modular quarantine bought perfect retention at the
   cost of new-task learnability here.

## Honest bounds (your discipline)

- Consolidation is pseudo-rehearsal / function replay: memory-free at deployment, but the frozen teacher
  is training-time memory + compute, and I query it on the old function's support (generated arith
  prefixes), so arith retention is expected-by-construction, not emergent regularization. I count the
  teacher as phase-B training memory. It is NOT literally replay-free during training.
- The grown arm's quad failure is confounded by adapter capacity/placement (r=16, FFN-residual, frozen
  trunk). A width/placement sweep might let it acquire quad — but even a successful grown arm would only
  MATCH consolidation while the joint oracle already shows capacity is not the bottleneck, so growth
  NECESSITY stays unproved regardless. I'll run the r-sweep only to characterize the growth arm honestly,
  not to rescue a necessity claim.
- held-family generalization is 0 everywhere (catalogue, not general rule) — consistent with the grokking
  probe expectation; I'll still report the grokking curve.
- Single seed, p=23; I'll add seeds and confirm the consolidation win replicates.

## What I plan next (unless you redirect)

1. Replicate consolidation across 2-3 seeds + report worst-case (not mean) A-retention and quad-acq.
2. Cost accounting you asked for: total params, trainable params/phase, phase-B train FLOPs (consolidation
   pays 2 forward passes/step for the teacher), active inference FLOPs (grown adapter vs shared), and state
   exactly what each arm needs at train/infer (teacher? task id? ownership table? none).
3. Adapter-width sweep for grown (does any bounded adapter over the frozen arith trunk acquire quad? if a
   large one does, report the crossover; if none does under a preregistered envelope, that's the frozen-
   trunk-insufficiency / retentive-rewrite frontier you flagged — and it would make consolidation, which
   DOES rewrite shared weights and succeeds, the clearly stronger mechanism).
4. Param-matched wide-from-start A→B control (isolate growth timing from parameter count).
5. Re-run the grokking held-delta probe (I had to kill it for GPU; single-token 80k at p=23, wd=0.05).

My reading: this is a clean positive for **consolidation into shared weights over modular growth** at this
scale, with capacity-sufficiency proven and growth-necessity refuted — consistent with the project's
standing negative on growth. Push back if you think the grown arm's quad-acquisition failure must be
resolved (via r-sweep) before I state "growth underperforms consolidation here", or if you want the
param-matched-wide control run before any comparative claim.
