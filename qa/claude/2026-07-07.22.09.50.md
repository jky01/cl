# R41 Rung 1 RESULT — STRONG PASS. Surprise-gated replay = deployable O(#exceptions) allocation.

Logs `docs/cloud_results/r41_rung1.{json,perqa.json,log}`, code `s3/wikibridge.py` (build_mixed/select_budget).
Pod reclaimed (LOCAL key now — moved reaper to a local cron per user; no key on pod) + deregistered. Pushed
ca3e048. Mixed stream 3×[3 squad + 3 synth], 1 seed, budget=50% old replayed. Old-only (60) para EM, split by
frozen-base bits/token (median 9.12):

| arm (replay 50%) | all-old | HIGH-bits (exceptions) | low-bits (on-manifold) |
|---|---|---|---|
| bgt_surprise | 0.617 | 0.533 | 0.700 |
| bgt_random | 0.500 | 0.267 | 0.733 |
| bgt_lowbits | 0.517 | 0.267 | 0.767 |
| bgt_sourceoracle | 0.583 | 0.500 | 0.667 |
| full (ceiling) | 0.617 | 0.500 | 0.733 |
| k0 (floor) | 0.417 | 0.233 | 0.600 |

Against your gates — all cleared:
- surprise − random = **+0.117 all-old, +0.266 HIGH-bits** (bars +0.10 / +0.15). PASS.
- lowbits (0.267 high-bits) ≪ surprise → allocation direction matters. PASS.
- sourceoracle competitive (0.500) and surprise BEATS it (0.533) → **frozen-base bits/token is a sufficient
  router; you don't need the source label.** This is the strongest part IMO.
- low-bits skipped safe (0.700 > k0 0.600); newest not underlearned (surprise final 0.589 ≥ full 0.578).
- **surprise@50% budget ties FULL replay on all-old (0.617) and beats it on exceptions (0.533>0.500)** — it
  spends the scarce budget exactly on the off-manifold tail and correctly skips on-manifold items.

Read: this turns the R40-s3 surprise result from a category marker into a **deployable allocation policy** —
spend O(#exceptions) committed-target budget routed by the model's own bits/token, not O(#facts). Rung 1
mechanism validated.

Two questions:
1. Density: at B=0.5 surprise already ties full, so tail ≤50% here (corpus is 50% synth by construction). To
   get the real density number I'll run the budget ladder B∈{0.25,0.5,0.75} + SEEDS=2 for sign-stability.
   Agree that's the immediate next run, or do you want SEEDS=2 at B=0.5 first (cheaper) to confirm the +0.27
   high-bits gap holds before spending on the ladder?
2. This corpus is artificially 50% off-manifold. The density that matters for "read books" is real-corpus
   off-manifold fraction, which we can't get from squad/synth. Do you think there's a cleaner way to estimate
   a REAL-text exception fraction (e.g. run bits/token distribution on genuine held-out book/wiki text and
   look at the tail mass above some retention-risk threshold), or is that a separate measurement?
