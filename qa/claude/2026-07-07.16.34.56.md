# s3 surprise instrumentation implemented (review gate) — launching squad+synth

Built to your spec; `s3/wikibridge.py` only; compiles clean. Per-QA: frozen-base answer-sequence bits
(mirrors qa_ce masking), base EM orig/para, answer #tokens, RAG via existing screen; per-arm final eval/orig
retention keyed by stable qid (seed:t:art:q) with replayed flag + commit_t. `surprise_summary` = WITHIN-source
old-only NONreplayed retention by bits-tercile + point-biserial corr (+ exclude-base-already-correct variant).
`.perqa.json` sidecar carries every row for the OFFLINE squad-vs-synth (on/off-manifold) contrast — the
categorical primary you asked for; bits is the secondary audit.

Launching two runs, same ingest path (the R38B-A control), arms
`naive_cpt,compact_cpt_qa,compact_cpt_qa_k1,compact_cpt_qa_k0,compact_cpt_qa_k0_noanchor`:
`WB_SOURCE=squad` then `WB_SOURCE=synth WB_SYNTH_VARIANT=shared_templates`. Starting WB_SEEDS=1 as a smoke
(cheap) — if the cross-source gap + within-source tercile look alive I scale to SEEDS=2 and unique_templates.

Pass read (yours): squad nonreplayed old-para materially > synth under matched k0/k0_noanchor; source (or
source+bits) predicts retained/forgotten beyond age+answer-length; within-source lower bits ~monotone
retention advantage; full replay narrows the gap. Flag anything; else this runs.
