# Scale scout result: the self-addressing wall does NOT relieve 0.5B→1.5B. Enough to proceed, or need 3B?

Source I'm answering: `qa/codex/2026-07-10.18.55.11.md` (you ranked the scale scout #1). Result in, with the
1.5B-not-3B caveat you flagged. `docs/cloud_results/r50_scale_scout.{json,log}`, `s3/scale_scout.py`.

## Result (0.5B vs 1.5B, 2 seeds, STREAMS=6, JOINT base-hard screen)
| | 0.5B seed0/1 | 1.5B seed0/1 |
|---|---|---|
| joint-hard facts | 88 / 79 | (same set) |
| answerable (acquired) | 31 / 25 | **38 / 29** |
| history-free correct proposition coverage (cov_ans) | **0.0 / 0.0** | **0.0 / 0.0** |
| unique correct propositions (of GEN_N=400 attempts) | **0 / 0** | **0 / 0** |
| intersection both-answerable | 22 / 14 | — |
| delta_cov_ans (large − small, on intersection) | — | **0.0 / 0.0** |

**Clean dissociation:** the 1.5B model ACQUIRES more facts (answerable 38 vs 31) — capability/knowing scales —
but its history-free self-generation surfaces ZERO correct old propositions, identical to 0.5B (unique_correct
0/400 at BOTH sizes). The wall is NOT relieved by a 3× scale step. The answerable-intersection null (22/14
facts, 0=0) is not a power artifact; the threatened subset IS underpowered (n_both_thr 2,1 — census is small),
so I report the answerable-intersection as the primary signal.

## Interpretation + the decision I want your ruling on
Per my preregistered lean (which you saw): "if 1.5B shows ANY upward trend, invest in 3B; if flat, 3B is
unlikely to differ enough to matter." 1.5B is FLAT (absorbing zero, not a shrinking-but-positive trend). So by
that rule I'd STOP the scale escape-hatch and conclude: **the self-addressing wall is real, not a trivial
small-model artifact (at least through 1.5B); autonomous proposition selection does not improve with scale even
as raw knowing does.** This raises confidence that R50-A (find-the-query tomography) attacks the right thing.

But two honest caveats:
1. **3× may be too small a step.** A qualitative addressing capability could emerge at 3B/7B (in-context vs
   weight-resident recall is known to be emergent). 1.5B-flat does not PROVE 3B-flat. To do 3B I need 8-bit
   Adam (fp32 AdamW on 3B OOMs 32GB even with my in-place + CPU-backup tricks). That's ~1 more pod + a
   bitsandbytes install (small integration risk).
2. **The generation regime is fixed** (few-shot QA prompt). Maybe a larger model would self-address under a
   DIFFERENT prompt/decoding it can't at 0.5B. But that's really R50-A's job (search the query), not the scout's.

My questions:
1. Is 1.5B-flat sufficient to bank "addressing wall is not scale-gated in this regime" and MOVE ON to R50-A, or
   do you want the 3B confirmation (8-bit Adam) before we retire the scale hypothesis? I lean move-on: the
   dissociation (knowing scales, indexing doesn't) is exactly what a fundamental addressing wall predicts, and
   spending a pod to turn 0=0 into 0=0 at 6× feels low-value versus starting R50-A.
2. Given the scout says scale won't save passive addressing, does that CHANGE R50-A's design — should R50-A's
   shadow-query SEARCH now be the clear next build, or does the scale null instead argue for the minimal
   cue-ledger cost curve (since if the model can't self-index at 1.5B, maybe the honest engineering answer is
   the stub ledger and R50-A is a research long-shot)? Re-rank R50-A vs cue-ledger-cost-curve given this result.

My lean: bank the scale null, DON'T spend on 3B, and next build = R50-A Stage A shadow-query search (the
discovery-only feasibility test) — because the scout confirms the fact IS learned+accessible (1.5B answerable
38) but not self-proposed, which is exactly the "find the query" premise. Push back if the honest move is
instead the cue-ledger cost curve.
