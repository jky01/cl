# R38B-A synth control: DECISIVE — the floor is a real-text effect, not objective gentleness

Ran it, 3 seeds, pushed 7bf19bb, pod reclaimed. Files: `s3/wikibridge.py` (`build_synth`, `WB_SOURCE=synth`),
`docs/cloud_results/r38ba_synth.{json,log}` (per-seed rows included). Matched R38B: 3 streams / 12 articles,
same run_ingest path, only the corpus differs.

## Result vs your pre-registered threshold

Old-only non-replayed old-paraphrase EM (decision metric):

| arm | synth 3-seed avg | synth per-seed | R38B real text |
|---|---|---|---|
| naive_cpt | 0.000 | — | 0.000 |
| k0 (+anchor, zero old replay) | **0.181** | 0.175 / 0.267 / 0.100 | 0.625 |
| k0_noanchor | **0.164** | 0.100 / 0.217 / 0.175 | 0.738 |
| k1 | 0.243 | — | 0.641 |
| k3 | 0.312 | — | 0.687 |
| compact_cpt_qa (full replay) | final 0.614 / replayed 0.664 | — | 0.80 / 0.787 |

Your threshold: k0/k0_noanchor <= 0.25 -> real-text effect; >= 0.55 -> objective gentleness. Synth k0/k0_no
= **0.181 / 0.164, all three seeds <= 0.27**. This is the **collapse** outcome. So the R38B ~0.65 no-replay
floor is a **real-text property** (prior-anchoring / redundancy), NOT the gentle objective. On independent
invented facts the identical objective preserves almost nothing without replay.

Sanity all held: base 0.000, RAG 1.000; naive 0.0; full replay retains; replay protects replayed items (k1
replayed 0.764 vs non-replayed 0.243) — R36-A rehearsal, no neighbor spillover. And since `shared_templates`
(max format sharing) already collapsed, it is **not** format/template sharing -> I did NOT run
`unique_templates` (your escape hatch is unnecessary; the disambiguation already came out negative).

## What this settles

Footprint thread closes with an honest split, which I've written into FINDINGS:
- STRONG claim ("K-replay buys sub-O(#facts) coverage via redundancy spillover") — **dead** (R38B).
- NARROW claim ("real / prior-anchored corpora leave a no-replay retention floor that independent facts
  lack") — **supported** (R38B-A). Mechanism: real facts overlap the pretrained manifold and resist
  interference even unreplayed; invented independent facts have no anchor and are overwritten unless replayed.

One honest asymmetry I flag: synth is globally harder (full-replay 0.61 < real 0.80), consistent with weaker
prior anchoring for invented tokens. So part of the gap is "invented facts are just more fragile" — but that
fragility IS the prior-anchoring mechanism, so it supports rather than confounds the narrow claim. Do you see
it differently — is there a cleaner way to separate "prior overlap" from "cross-fact redundancy" as the two
sub-mechanisms, or is that a distinction without a decisive experiment at this scale?

## Next move — I lean your B (fold + pivot to the actually-open frontier)

Per your earlier ranking (A done; then B; C opportunistic), I propose:

1. **Fold R37-A / R38-A / R38B / R38B-A into the capstone** as the honest continual-knowledge arc:
   real text -> in-weight closed-book QA (R38-A); per-item replay still required, no free redundancy
   spillover (R38B); but prior-anchored knowledge has a real no-replay retention floor independent facts
   lack (R38B-A); localized-write growth is load-bearing only under isolation (R37-A).
2. **Pivot to the genuinely-open frontier: rehearsal-FREE retention.** The R38B-A floor is suggestive — if
   prior-anchored knowledge resists interference *without* replay, the open question is whether we can
   *engineer* that anchoring for NEW knowledge (make new facts sit in an interference-resistant subspace) so
   retention no longer needs per-item rehearsal. That's the nswrite/null-space-write family (R36-I), but
   your own note said input-direction protection looks limited. So the real question: is there a NEW writer
   that protects the *answer function* directly (not just input directions) — e.g. consolidating new facts
   into a region orthogonal to the realized update directions of already-committed facts, measured by the
   exact-OGD realized-ΔΘ machinery we already built? I'd want to design this with you before building.

Do you agree B now (capstone + rehearsal-free design round), or is there a cheaper decisive experiment I'm
skipping — e.g. a scale check (does the real-text no-replay floor grow or shrink with model size / #streams),
which would tell us whether the narrow R38B-A positive strengthens or is a small-model artifact? Rank
"capstone+rehearsal-free-design" vs "scale the R38B-A floor" as the next pod.
