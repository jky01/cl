# R38 CF audit + FOOTPRINT sweep results — real-text redundancy partially softens the O(#facts) bound

Both done, pushed (ab08d61). Files: `docs/cloud_results/r38_cf.{log,json}`, `r38_footprint.json`,
`s3/wikibridge.py`. Both pods reclaimed.

## CF audit (base-ignorance guaranteed, edited-number counterfactuals)

- base O/P EM = **0.000/0.000** → base-ignorance confirmed.
- compact_cpt_qa **orig EM 0.938** → new/counterfactual passage knowledge IS written into weights over the
  base prior. Core CF question passes.
- BUT **RAG-gold para EM = 0.188** → my crude numeric-edit paraphrases are malformed (change answer type,
  as you warned) so the CF *paraphrase* metric is unreliable. This is a **data-construction** limit, not a
  method failure. So CF supports base-ignorance + orig-surface writing; it does NOT give a clean CF
  paraphrase claim. Honest.

## Footprint sweep (the key science — your Q1 on real-text redundancy)

K committed QA/article replayed; **replayed vs NON-replayed old-paraphrase EM** (R36-A split on real text):

| K/article | final para | replayed para | **NON-replayed para** | n_rep/n_non |
|---|---|---|---|---|
| naive (no replay) | 0.000 | — | — | — |
| 0 | 0.507 | — | **0.507** | 0/75 |
| 1 | 0.573 | 0.933 | **0.483** | 15/60 |
| 2 | 0.627 | 0.800 | **0.511** | 30/45 |
| 3 | 0.853 | 0.933 | **0.733** | 45/30 |
| all | 0.893 | — | — | — |

**Answer to Q1: real text PARTIALLY softens the R36-A O(#facts) bound.** In synthetic R36-A, non-replayed
old items → ~naive (≈0). Here non-replayed old QA hold **~0.50 at K=0–2** and **rise to 0.733 at K=3** —
genuine spillover to non-replayed stream-mates that independent synthetic facts could not have. Two effects:
(1) the gentle compact-consolidation preserves ~0.5 with ZERO old replay (K=0=0.507), far above synthetic
naive; (2) real-passage redundancy lets a small K generalize to non-replayed. **First evidence that "read
many books" may cost < O(#facts).**

## The confound I want your read on

The K=0 baseline (non-replayed 0.507 with NO old replay) muddies the attribution: is the redundancy
spillover real, or is most of the 0.5 just the consolidation being gentle (new-QA CE + neutral anchor,
starting from M_{t-1}, not a destructive full-CPT like naive)? To attribute cleanly I think I need:
1. a **naive-consolidate floor** — consolidate with new-QA CE + anchor but explicitly ZERO old signal AND
   no gentleness advantage (or measure how much K=0's 0.507 is "old facts written strongly during their own
   stream survive a gentle pass" vs "shared-structure spillover");
2. **≥2 seeds** (currently 1) and a **larger corpus** (only 15 articles survived para-screen);
3. **answer-type-diverse coreset** vs random-K (does smart selection beat random at fixed K?).

## Questions
1. Is the footprint result strong enough to claim "real-text redundancy softens the coverage bound"
   with the 1-seed/15-article caveat, or must I get multi-seed + the naive-consolidate attribution floor
   BEFORE the claim? I lean: report as PARTIAL/suggestive now, confirm with multi-seed + floor next.
2. Given where we are — R37-A (clean positive + growth-necessary), R38 (bridge works, para 0.893), CF
   (base-ignorance ✓, cf-para data weak), footprint (redundancy partially softens bound) — is the highest-
   value next move (a) **multi-seed + naive-consolidate floor** to harden the footprint attribution, (b)
   **scale** (more articles / 1.5B for the scale-trend), (c) **better CF data** (typed-answer QA that
   paraphrases cleanly) to close the base-ignorance-paraphrase gap, or (d) **consolidate** this R37-A/R38
   arc into the capstone as "real-text bridge + softened footprint" and pause? I lean (a) then (b): harden
   the footprint claim, then scale — but want your ranking.
3. Any methodological hole in the footprint sweep itself (e.g. the K=0 gentleness confound, or that
   committed-correct selection is by orig-question so replayed items are orig-easy) that must be fixed
   before the result is trustworthy?
