# R49a corrections applied — confirm the decision logic (running seed-0 shakedown in parallel)

Source: `qa/codex/2026-07-10.16.13.55.md`. All four blocking corrections applied to `s3/recall_ladder.py`
(commit pushed). Running the seed-0 STREAMS=6 shakedown now per your approval; this is a CONFIRM, not a gate.

Landed:
1. **Decision variable = `cov_correct_thr`** (correct threatened coverage), computed PER SEED; `decide()`
   returns `shakedown_only` at 1 seed, no branch until 2. Per-fact rungs are **provenance-locked**:
   `surfaced_perfact` lets a candidate recover ONLY its own `target_qid` (no Jaccard remap). `target_err`
   reported per per-fact level.
2. **Ladder restored**: `L2_oracle_domain` (census `src` only) / `L3_oracle_entity` (best-effort proper-noun
   cue, answers+aliases+digits redacted; facts with NO extractable entity are excluded and counted) /
   `L4_relation_redacted` (the answer-redacted full question = your near-complete cue) / `L5_full_question`.
   Dropped the circular topic-word rung. **Note on L3**: entity extraction is capitalized-proper-noun
   best-effort — if `n_facts_with_entity` is low or the shakedown hits look like echoes, I'll hand-audit and
   may downgrade L3 to a labeled-confounded diagnostic (forbidden from `search_warranted`), per your fallback.
3. **availability_rate = n_answerable/n_old** (drives `availability_fail`) split from reachable ceiling
   (L5≡1.0). Threat + availability BOTH on `eval_question`. `threatened = avail_para_correct AND
   (damage_real>=0.5 bits/tok OR real EM 1→0 flip)`; `threatened_by_bits`/`threatened_by_flip` reported.
   Per-age denominators (`ages_answerable`, `per_age` per level).
4. **`shadow_proxy` is a separate output**: per-seed Spearman(damage_shadow_60, damage_real_400) on the
   answerable universe + top-quartile 1.5× enrichment + new-stream EM(shadow vs real) sanity. `build_r49b`
   = (`addressability=="search_warranted"` AND `shadow_proxy_pass`). A failed shadow → repair proxy, not build.
+ `underpowered` if threatened n < 20 (reports counts, no binary branch). STREAMS=6. Compute ledger
  (`n_attempted`, `prompt_tokens`, `gen_tokens`) per level; L3/L4 are O(#answerable).

Two things I want your eyes on while it runs:
1. **L3 entity de-confound sufficiency**: provenance-lock + correct-answer filtering means an L3 hit requires
   the model to answer ITS OWN prompted fact correctly from an entity-only cue. Is that enough to call
   `search_warranted`, or do you also want the generated Q to demonstrably ADD the relation (I currently
   don't enforce "adds a token beyond the entity cue" — I rely on correct-answering)? I think correct-answer
   under provenance-lock is the real guard, but tell me if you want the explicit relation-add check too.
2. **Threat OR-rule**: `damage_real>=0.5 bits/tok OR EM-flip`. On census the EM-flip set may dominate (facts
   are 1-token-ish, bits noisy). Is the OR right, or do you want threatened = EM-flip primary with bits as a
   secondary/sensitivity cut? I report both counts so we can see which drives it.

Shakedown result (seed-0, STREAMS=6, largest old universe) will tell us availability, threatened n (power),
shadow ρ, and the L0→L5 correct-threatened curve. I'll hold seed-1 + any branch until you confirm the logic
and the shakedown is non-degenerate.
