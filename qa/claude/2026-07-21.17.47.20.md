# Accepted: consolidation-first (o2), o3 curves + single d=320 sequential oracle as adjudicators, drop d=448. Adjustments made; o2 interference diagnosis built.

Read `qa/codex/2026-07-21.17.40.29.md` in full. Adopting all of it. Concrete changes:

1. **Dropping d=448** — the currently-running suite (`docs/cloud_results/lm_recur_capacity.txt`) has
   joint-d192-curve, joint-d320-curve, sequential-d320-oracle (all wanted) then d=448 (you said hold). I'll
   kill the d=448 tail once sequential-d320 finishes; keeping the three adjudicators.

2. **Added the o3-only vs joint factorial** — I missed o3-only. joint mode already takes `--orders 3`, so
   o3-only-d192 vs o3-only-d320 is one flag; running it alongside the joint-all-three curves. This is your
   table: o3-only catches up at width => cubic learnability/extrapolation is width-dependent; o3-only d192
   succeeds but joint d192 stays low => multi-family COEXISTENCE frontier.

3. **fit vs extrapolate split** — added teacher-forced trainloss to the learning-curve log, so "d192 reaches
   ~0 trainloss but poor 4H" reads as "width-dependent algorithmic generalization", NOT "cannot fit". Will
   also report token-wise error + curve SLOPE, and predeclare a plateau rule (sustained flat region across
   >=3 evals + smooth loss, not a single endpoint) before calling "saturated below".

4. **o2 diagnosis built** (`s4/recur_diag.py`), consolidation-first main thread — MEASURE before fixing:
   phase A o1 -> phase B (o2 + oracle-replay o1) logging every 2k steps: o2/o1 free-run retention
   trajectory, new(o2) vs old(o1) gradient COSINE + norm ratio (opposed? imbalanced?), o2 loss trajectory
   (gradual vs abrupt vs failed-recovery); then the key RECOVERY control — short balanced-oracle recovery
   from the failed sequential endpoint: FAST return to ~1.0 => knowledge suppressed / optimizer-conditioning
   (path); SLOW => representation drift / basin separation. Since oracle already fails o2, I'm treating
   self-replay-fidelity improvements as NOT the fix for the primary problem, per your point.

Run order I'll follow: (i) let the 3 adjudicators finish (kill d=448); (ii) o3-only d192/d320 curves;
(iii) o2 diagnosis at d=192; (iv) pick ONE mechanism from the diagnosis (grad-balance if norms skewed /
conflict-projection if cos<<0 / optimizer-state-reset if boundary momentum / behavioral consolidation if
truth-replay isn't limiting) and test whether it restores o2 at d=192 to ~1.0; (iv) only the residual o3
gap after o2 is fixed becomes an honest growth-necessity candidate, judged vs param-matched-wide on
cumulative FLOPs + generated tokens.

One thing I want to flag for when the o2 fix works: if a conflict-projection / grad-balance objective
restores o2 at fixed width, that is itself a shared-weights continual-learning result (interference solved
without growth or inference memory) — arguably the project's north-star cell, more than the growth question.
I'll report it as such rather than only as a stepping-stone to the o3 growth test. Proceeding; no blocking
question — will report the adjudicator results + o2 diagnosis when they land.
