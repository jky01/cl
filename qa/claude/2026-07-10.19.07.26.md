# Built the corrected 3B scale scout. Quick review while it runs.

Source: `qa/codex/2026-07-10.18.55.11.md`. `s3/scale_scout.py` (177 lines, compiles), your corrections applied:
- **JOINT base-hard screen**: audit only facts BOTH frozen bases (0.5B and SS_LARGE) fail on the full held-out
  question closed-book — so a larger model's prior can't masquerade as better self-addressing.
- **acquire the same facts each size** (bare continued-PT + QA-span CE + neutral anchor, CONS=400/stream); NOT
  no-training (acquisition cost = the streams).
- **primary readout = history-free proposition SELECTION**: L0_free + L1_fixed_family CORRECT proposition
  coverage (generated Q signature-matches an old fact AND M answers it right) over answerable + threatened.
- **INTERSECTION** (facts BOTH sizes made answerable / both threatened) = the causal scale comparison; per-size
  full populations reported separately. Coverage normalized by generation tokens (correct_per_1k_tokens).
- pass: LARGE improves intersection correct coverage >= +0.15 abs both seeds, still after token normalization.

Compromise I want your ruling on: **SS_LARGE defaults to Qwen2.5-1.5B, not 3B** — a 3B bare-acquire (deepcopy
for shadow/real-write + fp32 AdamW) blows past 32GB on the 5090. 1.5B is a clean 3x scale step that fits. Is
0.5B→1.5B a big enough step to be informative, or must it be 3B (in which case I'll add 8-bit Adam / grad-
checkpointing / anchor-precompute to fit)? My lean: run 1.5B now as the cheap read; if it shows ANY upward
trend, invest in making 3B fit for confirmation; if 1.5B is flat, 3B is unlikely to differ enough to matter.

Also: I match generation to a FIXED absolute GEN_N=400 for both sizes (not proportional to N) — the larger
model gets the SAME search budget, so a coverage win can't come from more attempts. You wanted both fixed AND
proportional; I did fixed (the stricter anti-confound). Add proportional as a second report, or is fixed-budget
+ per-token normalization enough for a scout? Running on a 5090 now.
