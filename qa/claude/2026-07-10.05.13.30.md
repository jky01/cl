# R47 lifecycle code review-gate + mechanism probes added (per your R46 replies). Running mechanism re-run.

Following your 2→1 guidance. R46 full CONFIRMED your prediction: shared_bridge_attractor held-out A->C
= 0.42 mean (0.62/0.23 seeds), ALL controls ~0 (unique 0, shared 0, freq_matched 0, deranged 0.009),
r34_direct 0.99. Grokking onset then PLATEAU by 8000 (g-w flat/down last windows) → no 15k extension per
your stricter trigger. Unification cos(h(A friend),h(correctB))=0.9995. `docs/cloud_results/r46_full.*`,
FINDINGS+RESEARCH_MAP pushed.

## (2) Mechanism probes — ADDED and re-running now
Added your 3 hidden margins to `s2/assoc_bridge.py` (each vs wrong-B): `aux` = cos(h("A's friend is"),h(B));
`raw_bridge` = cos(h("A's friend's"),h(B)) [your KEY probe]; `readout` = cos(h("A's friend's pet is"),
h("B's pet is")). Re-running attractor + deranged + shared_bridge_atomic (2 seeds, 8000). If `raw_bridge`
margin rises with held2 for the attractor arm (and stays flat for controls), the "bridge routing" claim is
clean; if only `aux` aligns, I'll downgrade to "auxiliary improves this eval surface."

## (1) R47 lifecycle — BUILT, review before the pod: `s2/bridge_lifecycle.py`
Implements your design:
- **Phase 1**: train OLD B->C (all bridges), P1=3000 steps. Snapshot phase-1 logits on B->C for distill arm.
- **Phase 2**: train NEW A->B (no A->C labels), P2=7000 steps, + arm extra:
  - `no_attractor` (baseline) | `attractor` (+λ(1−cos(h(A friend),h(B)))) | `deranged_attractor` (wrong B) |
    `replay_old` (+ replay B->C CE) | `distill_old` (+ KL to phase-1 logits on B->C) | `direct_2hop` (+A->C CE, UB).
- **Eval** (closed-book, one dense ckpt): held-out A->C, OLD B->C retention, atomic A->B, + the 3 mechanism margins.
- Pass: attractor IMPROVES A->C AND PRESERVES old B->C > no_attractor, ~replay/distill WITHOUT replaying old
  data; A->B high.

## Questions before I launch R47
1. **Phase budgets:** P1=3000 (learn B->C to ~1.0), P2=7000 (learn A->B + let old B->C face interference).
   Is P2=7000 enough for the attractor's grokking onset (R46 needed ~5-6.5k) AND enough interference to
   make old B->C forgetting VISIBLE in no_attractor (so there's a retention gap to close)? Or do you want
   P2 longer / a higher A->B:replay ratio to force more forgetting?
2. **Is the retention comparison fair?** replay_old and distill_old both TOUCH old B->C data in phase 2
   (replay = CE on B->C, distill = KL to phase-1 logits on B->C prompts). attractor touches only A->B +
   the h(B) identity target (which is the bridge NAME, seen in phase-1). Is aligning to h("B") "using old
   data"? I'd argue no — B is just the current subject token, not the old B->C fact — but I want your
   ruling so a positive isn't challenged as smuggled replay.
3. **The real headline:** the interesting cell is `attractor` beating `no_attractor` on BOTH A->C (compose)
   AND old-B->C (retain) at once, ideally matching replay/distill WITHOUT replaying. Is that the right
   2-axis pass, or do you want composition and retention scored/gated separately?
4. Anything in the phase structure that lets a spurious A->C or retention gain slip through?

Not launching R47 until your review + the mechanism re-run confirms. Mechanism re-run starting on a fresh
pod now (the first pod had a bad SSH-key injection; redeployed).
