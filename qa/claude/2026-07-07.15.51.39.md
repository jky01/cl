# R40 implemented + pushed (944a0be) — launching the smoke

Your review-gate blockers were already handled in the working tree you hadn't seen yet; confirming each +
the 4 refinements you asked for. Changed file `s2/lifecycle_bakeoff.py` only; compiles clean.

Blockers (all done): (1) merge_sum/merge_ties in flags + _scaf_arms + dispatch elif; (2) surprise_probe
called per-seed after stream build + `.perfact.json` sidecar written; (3) perfact_seen/para stashed in
run_nswrite (so naive_fixed + nswrite participate), also run_consolidate/run_loramerge/run_mergeparallel.

Your 4 refinements applied: (a) per-mode lambda `BK_MERGE_SUM_LAMBDA`(0.5)/`BK_MERGE_TIES_LAMBDA`(1.0);
(b) merge arms flagged `uses_training_state=True` (deltas are retained merge-time state, honest); (c)
surprise_summary now ALSO reports the exclude-already-correct variant (so the signal is write-difficulty,
not "base already knew it"); (d) sidecar carries explicit fact tuples as IDs.

merge delta storage: exact dense q/v `merged-base` diffs (your Q1), reported honestly via `delta_store_bytes`
— NOT described as rank-sized. VRAM: base+scratch+PEFT resident (~10GB fp32) — fine on the pod; I'll optimize
to CPU-base only if merge looks alive. TIES density 0.2, disjoint mean, no sweep in smoke (your Q2). No
merge_shuffle_sign in first smoke (your Q3).

Launching your exact smoke: `BK_ARMS=naive_fixed,nswrite,ours_tgt_answerid,loramerge,merge_sum,merge_ties
LD_ROUNDS=3 LD_PER=20 LD_SEEDS=1 LD_STEPS=600 BK_DATA=kg`. Gates as pre-registered (surprise predicts
retention incl. excl-already-correct; merge_ties>loramerge baseline, ≥nswrite+0.05 frontier; retained/bit).
Will report numbers + reclaim pod. Flag anything; otherwise this is running.
