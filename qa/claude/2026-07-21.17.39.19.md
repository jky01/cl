# Joint-oracle adjudicator: TWO bottlenecks cleanly separated. o2's degradation is OPTIMIZATION/interference (joint d=192 fits it at ~1.0); o3 is genuine WIDTH-DEPENDENT CAPACITY (joint o3 rises monotonically 0.15→0.57→0.64 with d=192→320→448). This is the first width-dependent frontier in the project.

File `s4/recur_recursive.py --joint`, log `docs/cloud_results/lm_recur_joint.txt`. 3-family joint oracle
(train o1+o2+o3 together, balanced sampling, 48000 steps = 3×16000 to match total sequential compute),
width sweep, free-running exact H/2H/4H:

```
JOINT-ORACLE      o1 arith        o2 quad         o3 cubic
d=192  1.79M      1.00/1.00/1.00  1.00/1.00/0.98  0.60/0.22/0.15
d=320  4.95M      1.00/1.00/1.00  0.99/1.00/1.00  0.86/0.75/0.57
d=448  9.68M      1.00/1.00/1.00  0.99/0.99/1.00  0.93/0.81/0.64
```

## Reading against your decision matrix

This is the "joint d=192 partially fails, wider joint improves" row -> a **width-dependent
capacity/learnability frontier**. But it decomposes into TWO different bottlenecks, cleanly separated:

1. **o2 (quad): NOT capacity.** Joint d=192 already fits o2 at 1.00/1.00/0.98. So the recursive experiment's
   o2 degradation (self 0.53, oracle 0.32 at 4H) is **sequential optimization/interference**, not an
   absolute width limit — the width CAN represent o2 alongside o1 and o3; the sequential+replay path did not
   find it. Fix = better consolidation objective / gradient-conflict handling, NOT growth.
2. **o3 (cubic): width-limited.** Even perfect joint training at 48000 steps caps o3 at 0.15 (4H) for d=192,
   and it rises MONOTONICALLY with width (0.15 → 0.57 → 0.64 for d=192/320/448). This is the project's first
   signal of a genuine capacity-dependent ceiling: more active width buys more exact cubic extrapolation.

So the same experiment shows an optimization ceiling (o2) and a capacity ceiling (o3) simultaneously, and
the joint oracle disentangles them.

## What I am NOT yet claiming (your discipline)

Per your matrix this is a growth CANDIDATE, not growth necessity. Two confounds remain for o3:
- **Convergence**: is o3 at d=192 "cannot fit" or "did not converge in 48k steps"? o3 joint is still rising
  at d=448, so none of these may be converged. I need a converged-budget check (e.g. 4x steps at d=192 and
  d=320) to confirm o3 d=192 is truly capacity-capped, not undertrained.
- **Sequential vs joint at width**: does a WIDER model in the SEQUENTIAL setting reach the wider-joint o3
  retention? If wider-sequential also fails where wider-joint succeeds, width alone isn't the fix and growth
  needs an isolation/consolidation mechanism, not just more params.

## Proposed next runs (your matrix, in order)

1. **Converged o3 control**: d=192 and d=320 joint at 64k steps (o3 only, or all-3) to separate
   capacity from convergence. If d=192 o3 saturates well below d=320, capacity-limit confirmed.
2. **Wider sequential oracle**: recur_recursive rehearsal=oracle at d=320 (and d=448) — does extra active
   width relieve the SEQUENTIAL o3 (and o2) degradation? This is the decisive "does growth have a functional
   role" test: wider-joint succeeds AND wider-sequential succeeds => width relieves continual interference;
   wider-joint succeeds but wider-sequential still fails => size alone insufficient, need isolation.
3. **Paired-seed self/oracle replication** (your prior ask): 3 seeds, identical init/data pairing, per-family
   /horizon deltas, predeclared equivalence margin, to promote "self matches oracle" from run-level to
   method-level. Plus the 3-axis fidelity decomposition (truth fidelity / old-new behavioral agreement /
   task knowledge) + rollout-survival curve.
4. Only after 1-3: the growth arm vs param-matched-wide, judged on cumulative train FLOPs + generated tokens
   (npool×F, quadratic cumulative), not just final params.

Two questions:
(a) For the o3 capacity claim, is a converged-budget check at fixed width (4x steps) sufficient to rule out
    undertraining, or do you want a learning-curve (o3 vs steps at d=192 vs d=320) so "saturated below" is
    explicit rather than a single endpoint? I lean the learning curve — one number can't show saturation.
(b) Given o2 is an optimization ceiling not capacity: is the more valuable thread now (i) fixing the
    sequential consolidation objective so o2 (and maybe o3-within-capacity) is retained under the recursive
    protocol — i.e. attack interference directly, the project's rehearsal-free-ish north star — or (ii)
    chasing the o3 growth-necessity proof? I lean (i) FIRST (o2 says our consolidation loss is leaving
    representable retention on the table; fixing it is the shared-weights win), THEN (ii) for o3 where
    capacity genuinely binds. Growth is only honestly motivated for the residual that capacity truly can't
    hold after the consolidation objective is fixed.

I'll run #1 (converged o3 learning curve) + #2 (wider sequential oracle) now unless you redirect; they
together decide whether o3 is capacity or convergence, and whether width relieves sequential interference.
