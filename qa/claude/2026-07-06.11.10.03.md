# R36-A: hygiene confirmed, sweep is RUNNING (valid); non-replayed gate labelled provisional

Quick confirmation on your two review-gate points — the running sweep already satisfies both, so I let
it proceed rather than restart. Files: `s2/lifecycle_bakeoff.py`, driver writes `docs/cloud_results/`.

1. **PYTHONHASHSEED=0** — already set. My sweep driver `export PYTHONHASHSEED=0` at the top, inherited
   by all 8 per-K `python -m` processes. So `hash(kind)` is deterministic across jobs; minibatch/surface/
   anchor draws differ only by the intended kind-seed, not by process. (I'll still swap `hash(kind)` for a
   stable offset map in a follow-up commit for robustness, but it does not affect this sweep.)
2. **Nested subsets** — confirmed already nested (one stable per-`(seed,stream)` permutation, first-K
   prefix; K-independent RNG). My earlier "independently-drawn" wording was wrong; the code is the nested
   prefix you want. I'll log the permutation seed / selected indices for auditability.

**Q2 non-replayed reference** — accepted; I did NOT pre-add the full-ours same-complement eval, so per
your allowance I run the sweep now and treat the non-replayed comparison as **provisional**, using
full-ours per-stream age-curve (streams 0..ROUNDS-2) as the proxy reference. **If K≤8 clears the primary
gates** (all-seen ≥ full_ours−0.06, all-para ≥ 0.72, oldest-forget ≤ 0.08, newest ≥ full_ours−0.03,
base-hop drop ≤ 0.03), **I will then run the exact same-complement full-ours reference**
(`full_ref_nonreplayed_seen_K{4,8}` via the same nested permutation) before making any R36-A claim. No
"minimal-footprint works" claim from the all-stream reference alone.

**Q3 compute-footprint** — deferred, agreed; first sweep holds `Bf`/replay-frequency fixed (storage-
footprint only). I'll report replay-example counts (`K=0`: 0; `K>0`: `Bf*STEPS` per non-empty-prior
round). Reduced-batch/frequency compute sweep only after a storage knee at K≤8.

**Running now (5090, expandable_segments):**
```
REF: BK_ARMS=naive,oracle BK_REPLAY_K=-1
sweep: BK_ARMS=ours BK_REPLAY_K in {40,16,8,4,2,1,0}   (K=40 == full-ours ceiling, K=0 == naive floor)
LD_ROUNDS=6 LD_PER=40 LD_STEPS=1000 LD_SEEDS=2, PYTHONHASHSEED=0
```

Interpretation I'll apply (yours): K≤8 passes primary + same-complement non-replayed ⇒ real minimal-
footprint-into-weights (not rehearsal-free, not a growth claim); replayed-high/non-replayed-lag ⇒ item
rehearsal not stream protection ⇒ pivot to coreset / key-only-generated probes; flat K0..K8 near naive ⇒
next lever is smarter selection or learned preservation writer, not more OGD or joint retrain.

The smoke already previewed the risk: at K=4 (tiny 2×20/60-step) REPLAYED seen 0.75 but NON-REPLAYED
0.06 — i.e. item-rehearsal, not stream-protection. If that holds at 6×40×1000, R36-A is a (useful)
negative and the key-only/coreset branch is next. Will report with all paths when the sweep lands.
