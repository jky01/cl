# R46 SMOKE: positive early signal for the bridge-attractor primitive. Full run launched.

Applied all your review fixes (dropped trainable proj head → direct cosine; ATTR=identity primary;
wrong-C prob + goldC−wrongC margin; ATTR-aligned hidden-sim). Smoke (bridge=12, aper=4, 600 steps, 1
seed, identity) plumbing works AND already shows your predicted pattern:

| arm | atomic | held2 A->C | hsim margin |
|---|---|---|---|
| unique_bridge_atomic (R32 floor) | 1.0/1.0 | 0.0 | 0.033 |
| shared_bridge_atomic (hub only) | 1.0/1.0 | 0.0 | 0.039 |
| **shared_bridge_attractor (identity)** | 1.0/1.0 | **0.167** | **0.632** |
| deranged_attractor (wrong bridge) | 1.0/1.0 | 0.0 | -0.044 |
| freq_matched_unique | 1.0/1.0 | 0.0 | 0.033 |
| r34_direct_2hop (upper bound) | 1.0/1.0 | 0.917 | 0.276 |

Reading (smoke only, 12 bridges/600 steps/1 seed — NOT a result): R32 floor reproduced (unique/shared/
freq all 0.0); hub multiplicity ALONE = 0.0; ONLY shared_bridge_attractor gives nonzero held-out A->C
(0.167) with a huge hidden-sim margin (0.632 vs ~0.03), and the deranged control cleanly fails (0.0,
negative margin). This is exactly your "if only shared_bridge_attractor works → concrete rehearsal-free
consolidation primitive: hidden-state bridge unification."

Full run launched: BRIDGE=60, APER=6, STEPS=8000, SEEDS=2, all 6 arms, ATTR=identity,
`docs/cloud_results/r46_full.json`. ~2-3h.

## Questions while it runs
1. **If the attractor arm's held2/gold-prob is still RISING at 8000** (grokking-like, per R34's long
   onset), you said extend to 15k on evidence. I'll watch held2_gold_minus_wrong + hsim margin; if
   rising, I'll relaunch that arm (+deranged) to 15k. Agree that's the trigger, and 15k the target?
2. **Interpretation guard:** the attractor loss aligns h("A's friend is") to h("B"), and held-out eval is
   "A's friend's pet is". So a positive is: bridge-unification at the friend-readout position transfers to
   the 2-hop-prefix position. Is that transfer itself the interesting claim, or do you want me to ALSO log
   whether the attractor arm's held2 gain survives if I evaluate the RAW 2-hop prefix state alignment
   (i.e., is the win because the model now routes "A's friend" through B's identity)? I can add a probe:
   cos(h("A's friend's"), h("B")) at eval — the actual 2-hop bridge position — as a mechanism check.
3. **If full confirms attractor >> controls:** what's the honest CLAIM ceiling? I'd frame it as "a
   training-time, weights-only, no-A->C-label consolidation primitive (hidden bridge unification) induces
   SOME latent 2-hop composition where independent bindings give exactly 0" — NOT "composition solved."
   And the natural next step = does it survive in a continual/lifecycle setting (old-hop retention) + does
   it beat random@0.5 on a knowledge-retention axis, or is it purely a composition-axis result? Your view
   on the honest scope if it holds.

Not blocking the run on these — just want your read for when it finishes.
