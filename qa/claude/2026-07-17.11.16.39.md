# New direction (user): give the model forward/length extrapolation. Proposal + review request: graded length diagnostic first, then a method ladder. The acquisition question is settled, so length-gen is now licensed.

Source: `qa/claude/2026-07-17.11.16.39.md`. Review-gate before I build the method arms. User wants the
capability they care about most: the model continuing/deriving the sequence BEYOND the training range —
i.e. the length-extrapolation wall that gate-1 exposed (copy=0 at [16,40]) and gate-2 isolated as
orthogonal to recurrence acquisition. Gate-2 passed (recurrence is a genuine algorithm in weights), so
per your ordering length-gen is now the licensed question.

## Framing (connected to tonight)

Extrapolation = the model EXECUTING the rule step-by-step vs EMITTING a fixed-length template. csum_reset
already generalized in recurrence HORIZON (reset-distance to 11) within length ≤12, so the state-update
computation is learned; the length-40 failure (even copy) is that output length is a template and
positions 13–40 are untrained. The fix should make the per-step computation position-invariant so
rollout length is emergent, not a template.

## Step 1 — graded length diagnostic (cheap, characterize the failure mode before choosing a fix)

On the suf_erm model, eval copy and csum_reset at lengths 12,14,16,20,30,40. Report per length:
sequence exact-match, token accuracy, and OUTPUT-LENGTH correctness (does it emit the right number of
tokens?), plus token-acc by absolute position. This distinguishes: (i) immediate cliff at 13 = pure
absolute-position artifact; (ii) graceful decay = partial length-gen; (iii) wrong output length = a
stop/format failure separate from the per-step computation.

## Step 2 — method ladder (cheapest first; I want your pick + ordering)

1. **Train-time position randomization / random offset**: shift each example by a random absolute
   position offset so positions 13–40 ARE seen during training (via offset) and absolute position
   cannot be relied on. Cheapest; directly tests whether the failure is an absolute-position artifact on
   top of rotary.
2. **Wider length curriculum** (your controlled (b)): train [3,24], test [28,60], with copy control,
   normalized by tokens/FLOPs. Distinguishes "moved the cliff" (enlarged interpolation) from "normalized
   extrapolation improved" (real length-gen).
3. **Explicit-state scratchpad**: reformulate so the target makes the local step manifest — the model
   reads the previous emitted state + next input and writes the next state; length becomes emergent.
   Strongest known for algorithmic length-gen, but changes the I/O format (must be disclosed; the "no
   inference memory" claim still holds since the scratchpad is the model's own output, not retrieval).
4. NoPE / index features — hold unless 1–3 leave a specific gap (you flagged not to start here).

## Three questions

1. Given rotary is already relative, is **position randomization (arm 1)** the right first causal probe,
   or would you go straight to the **scratchpad (arm 3)** since it's the principled "execute step-by-step"
   formulation that our whole framing points at? I lean run the diagnostic + arm 1 first (cheap,
   isolates the absolute-position artifact), then arm 3 as the principled fix if arm 1 is insufficient.
2. Is length extrapolation of an ALGORITHM (csum_reset) the right target, or do you want me to ALSO
   include the user's literal "number sequence" cases (parity/quadratic continued to larger numbers) so
   the result speaks directly to their question? I lean: keep it on csum_reset (clean, we have the
   isolating gate), and separately note that quadratic MAGNITUDE extrapolation is a distinct harder axis
   (unbounded output) we already showed needs a constrained hypothesis class.
3. For the scratchpad arm, does it count as "the model deriving the rule" or is it "we supplied the
   step decomposition"? I think it's the latter and should be labeled honestly — scratchpad supplies the
   compute structure; the model still learns the step. That's a legitimate capability result (forward
   extrapolation) but not autonomous rule discovery. Agree on that framing?

Running the graded diagnostic now (it's cheap and doesn't commit to a method). Will report before
building the method arms.
