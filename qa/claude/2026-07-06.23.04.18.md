# R37-A 2-seed result: decoy = minimum-viable partial positive (below nswrite); oracle paradox ROBUST

Log `docs/cloud_results/r37a_grow_local.log` (+ JSON). 2-seed 6×40, λ=1.

| arm | all-seen | oldest-forget | newest | delta_old_final | delta_old_curve |
|---|---|---|---|---|---|
| naive | 0.387 | +0.738 | 0.887 | — | — |
| ours_tgt_answerid (replay) | 0.875 | +0.000 | 0.887 | — | — |
| grow_local_anchor | 0.329 | +0.775 | 0.887 | 0.078 | [0,.146,.136,.094,.172,.078] |
| **grow_local_decoy** | **0.488** | **+0.550** | 0.887 | 0.054 | [0,.113,.134,.084,.361,.054] |
| grow_local_oracle | 0.177 | +0.887 | 0.887 | **0.001** | [0,.032,.014,.001,.001,.001] |

## Reads

**decoy is a real but MINIMUM-VIABLE partial positive.** all-seen 0.488 vs naive 0.387 = **+0.101**
(meets your +0.10 gate), oldest-forget +0.550 vs +0.738 = **−0.19** (meets −0.15), newest 0.887 = naive
(plasticity intact), base-hop drop 0.009. BUT it is **below the R36-C `nswrite` region (0.673)** and far
below replay (0.875). So localized-write is a *new, weaker* member of the bounded rehearsal-free family —
it does not "move toward nswrite," it lands below it. Honest status: partial mechanism, not a project-valid
solution. Also note `delta_old_curve` for decoy spikes to 0.361 at r4 then drops — the locality is noisy
across the horizon, not a clean monotone suppression.

**The oracle paradox is ROBUST at 2 seeds and striking.** oracle drives `delta_old` to **0.001** (grown
block essentially *identity* on old prompts — perfectly localized) yet retention is the **WORST** (0.177,
forget +0.887, old streams ~0.03). And retention is **non-monotone in footprint**: decoy (delta_old 0.054)
> anchor (0.078) > oracle (0.001). newest is 0.887 for ALL of them, so it is NOT a newest/plasticity
collapse — the grown block being forced to zero on old prompts *removes a contribution old-fact recall
actually needs*. This is exactly your "hidden-footprint identity ≠ answer preservation," now a robust
sub-finding: **the grown blocks are not inert on old prompts; zeroing their footprint destroys old
recall.** (Echoes R36-C: input/footprint-space identity ≠ answer-space preservation.)

## Plan (running + proposed)

1. **NOGROW control RUNNING now** (`BK_LOCAL_NOGROW=1`, decoy+anchor, 2-seed) — your required growth check.
   Prior: since decoy (0.488, with growth) is already **below** nswrite (0.673, *fixed capacity*), growth
   is almost certainly not the driver; NOGROW will likely match decoy → "locality regularization, not
   growth" (consistent with R23/R31/R32/R34). Will confirm.
2. **Answer-logit-drift bridge** (your proposal, justified by the robust oracle paradox): add
   `BK_LOCAL_METRIC=hidden|logit`. `logit` penalizes **KL(dense ‖ pre-round snapshot)** on the reference
   (decoy = valid; oracle-old = invalid upper bound) instead of hidden-norm. This tests whether protecting
   the *answer distribution footprint* (not hidden displacement) is the right causal quantity — the direct
   test of the non-monotonicity finding. One pod. If clean `decoy_logit` beats `decoy_hidden` and
   approaches nswrite, R37-A gets a real mechanism; if only `oracle_logit` works, it needs old-prompt
   coverage (= replay-like) and is not a valid no-replay solution.

Question: given decoy(hidden) lands **below nswrite**, do you still want the logit-bridge pod, or is the
honest call that R37-A is a *bounded partial positive weaker than nswrite* + a clean "footprint ≠ answer
preservation" sub-finding → write it up and keep the logit-metric as R37 future work? I lean: run NOGROW
(already going) + ONE logit-bridge pod (the oracle paradox makes it the highest-information next test), then
write FINDINGS R37-A regardless. If logit-decoy also lands below nswrite, R37-A closes as a bounded partial
and the frontier verdict stands: no cheap rehearsal-free/growth mechanism beats replay at this scale.
