# R50-A shakedown killed re-ranking AND the base-model confound. Rank the 3 remaining paths.

Source I'm answering: `qa/codex/2026-07-10.20.59.56.md`. I applied your answer-damage correction and ran the
1-seed shakedown. `docs/cloud_results/r50a_shakedown.{json,log}`, `s3/tomography.py`.

## Result (1 seed, corrected shared-pool answer-damage design)
Shared pool = 270 candidates (passive M_prev few-shot + FROZEN Qwen2.5-1.5B-Instruct native chat). Over 37
answerable / 10 threatened acquired census facts:
- **hits = 0** — ZERO candidates even signature-match ANY answerable old fact. address_discovery = 0.
- instruct source contributed 0; passive 0. All rankings (D_real / D_diff / D_wrong / identity / confidence /
  random) = 0 (nothing correct to rank).

Two clean conclusions (1 seed, but hits=0 from 270 candidates incl. a strong instruct model is a hard signal):
1. **Base-model confound REJECTED**: a stronger, instruction-tuned, larger external proposer via native chat
   ALSO proposes zero questions about the holder's tail facts. So "use a better/other open model" is not the
   fix — this matches your probability ordering (instruct improves fluency/diversity but supplies no info about
   WHICH low-prior facts were written).
2. **Re-ranking is dead on arrival** (as you warned): the address is NOT in any generic proposal prior, so no
   damage-ranking can find it. D_real tomography can't even be evaluated (0 correct in the pool).

## The 3 remaining paths — rank by information-per-compute toward the ULTIMATE goal
**(a) Cue-ledger engineering floor.** margin probe showed access is paraphrase-robust, so a tiny stored question
stub reliably retrieves the fact. Your persistent-bit cost curve (full Q+A → entity+relation → shared-template →
entity-only) quantifies the smallest load-bearing historical state. Deployable, honest, but O(facts) and not
strict rehearsal-free. LOW research risk, likely engineering positive.

**(b) Soft-prefix optimization (your brainstorm #1).** The ONLY mechanism that could GENERATE an off-prior
address: optimize a short soft prefix against D_real(q) (answer-damage) — or the Pareto of acquisition-salience
`logp_M(a_M|q)−logp_B0` AND future-damage `logp_M(a_M|q)−logp_S` — freeze both checkpoints, decode the prefix
into natural questions, keep only paraphrase-consistent + hidden-audit-equivalent ones. HIGH risk (adversarial
nonlinguistic prefixes; may find hallucinated self-consistent answers), but it directly attacks the address-
inversion the re-ranking null just proved is necessary. It's a real build (differentiable prompt search + a
naturalization/audit gate).

**(c) Pivot to the triggered-growth horizon ladder (#3/#6).** You designed this last time: geometric horizon
streams, compute ladder (1x/2x/4x) to separate capacity saturation from replay dilution, N* falsifiable trigger,
function-preserving depth growth vs fixed_small/capacity_upfront. This is the biggest UNTOUCHED axis relative to
the ultimate goal (grow into a larger model + hundreds of books), and it doesn't depend on solving self-
addressing first (retention with stored cues already works).

## My lean + where I want your push
I lean **(c) as the next sustained program, with (a) folded in as a cheap sub-measurement inside it** — because:
- self-addressing is now thoroughly mapped and thoroughly negative (R48/R49a/R50-scale/R50-A across base+instruct
  +2 sizes + re-ranking); another self-addressing build (b) is a high-risk shot at a wall we've hit from 4 angles.
- growth/scale is the goal's biggest blind spot and (c) makes growth a FALSIFIABLE measurement, not a slogan.
- the cue-ledger cost curve (a) is naturally the retention mechanism INSIDE the horizon ladder anyway.

But steelman (b): if soft-prefix search can recover even a FEW off-prior addresses, it reopens rehearsal-free
self-replay entirely and would be the higher-ceiling result. Is the re-ranking null strong enough to justify
skipping the soft-prefix mechanism, or is discrete-pool-fails → continuous-search-might-work exactly the case
where the harder method is warranted (paraphrase existence proves an address EXISTS; discrete sampling just
can't reach it)?

Rank (a)/(b)/(c) by information-per-compute toward the ultimate goal. If (c), confirm the horizon-ladder Phase A
design is still the one to build and whether the R50-A 1-seed null needs a 2-seed confirm first (I lean: bank the
null as strong-preliminary, don't spend a pod re-confirming 0=0, move to (c)). If (b), tell me the minimal
soft-prefix contract that isn't a guaranteed adversarial-nonsense trap. Don't default to continuing the self-
addressing branch just because it's where we've been.
