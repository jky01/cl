# Review-gate: `s3/selfannotate.py` (A0 built to your spec). Running preflight in parallel.

Source: `qa/codex/2026-07-11.06.44.48.md` (v2 ratified; A0 first). 288 lines, compiles. Your 6 arms:
- `passive` (M_t few-shot QA floor) | `contrast_q` | `contrast_qa` (channel split: per-candidate d_q = mean
  Δlogp(question|scaffold) and d_a = mean Δlogp(answer|question), M_t vs base, logged in rows) |
  `contrast_decl` (declarative scaffold; statement counted only if 3B judge says it expresses the gold fact
  AND the normalized gold answer string appears in it) | `source_annot` (passage-conditioned QA generation =
  the practical READ-step headline; passage truncated 1200 chars, per-passage budget GEN_N/len(passages)) |
  `gold` (ceiling + UTF-8 ledger-bytes reference).
- FROZEN manifest before generation: base wrong on canonical question AND held-out paraphrase (both views).
- One ordinary write: M_t = base + bare_write(stream) (CONS=400). Acquisition count reported.
- Audit: Jaccard signature-match (vs question+answer text) → 3B proposition-equivalence → M_t correct on the
  generated q → BASE WRONG on the same generated q → answer stable under ≥1 of 2 independent 3B paraphrases.
- Gate per your spec: coverage ≥0.25 AND precision ≥0.80 AND ≥4 unique props, each seed. Dominance + bytes.
- Contrastive: token-loop score = logp_Mt + λ(logp_Mt − logp_base), top-p 0.95 on M_t, λ=2.0, temp 0.9.

Deliberate simplifications to flag (veto if load-bearing):
1. M_prev = the ORIGINAL BASE (one ordinary write), not a mid-lifecycle checkpoint — cleanest delta for the
   science question; the realistic "write onto an already-written checkpoint" version is the promotion step.
2. Answer-stability gate is ≥1-of-2 paraphrases (not 2-of-2) to avoid the paraphraser's noise dominating at
   this scale; stability is reported per-row so we can tighten offline.
3. `contrast_q` and `contrast_qa` share one generation pass (parse question-only vs full pair) — same budget,
   two readouts; d_q/d_a computed only for the QA arm.
4. Signature matching for declaratives uses question+answer concatenation as the reference text.
Running 2-seed on a 5090 now (GEN_N=200/arm). Will not bank without your read; hard stop if all arms miss.
