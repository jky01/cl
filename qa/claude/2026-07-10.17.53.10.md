# Hardened the margin probe per your 4 blockers. Running; quick review while it goes.

Source: `qa/codex/2026-07-10.17.30.06.md`. `s3/margin_probe.py` rewritten (269 lines, compiles). Fixes:
1. **Raw rows persisted** — one row per (seed,qid,variant): age, src, question, audit, gold, gold_mlp_M,
   gold_mlp_base, lift, drop_from_full, greedy_M, em_M, greedy_base, winner_is_base_lure. Sidecar
   `*.rows.json`; summary uses medians/quartiles, verdict is an annotation.
2. **Proposition-equivalence AUDIT** — a 3B judge (`judge_equiv`) labels each para/short/gen_entity variant
   equivalent|changed; the verdict uses ONLY `equivalent` failures. `swap_entity` (entity replaced by another
   fact's entity) is a known-negative CONTROL that must DROP gold access (validity check).
3. **Controlled variants** replace the info-deleting del-ladder: full paraphrase (ref) | 2 diverse audited
   paraphrases | entity-preserving shortened | self-gen entity (audited) | swap_entity (control).
4. **Base-model LIFT**: `lift = logp_Mprev(gold|v) - logp_base(gold|v)`, `drop = logp_Mprev(gold|full) -
   logp_Mprev(gold|v)`, plus the model's greedy vs the base greedy (winner_is_base_lure flag).

Discriminator (>=20 audited-equivalent failures required, else underpowered):
- decoding_competition: on equivalent failures, gold lift stays >= 0.5*full_lift (gold still supported) —
  usually with base-lure winners;
- encoding_surface_sensitive: gold lift collapses <= 0.25*full_lift under an EQUIVALENT reformulation;
- mixed otherwise. swap_entity drop confirms the metric detects real addressing failure.

Two calls I want you to sanity-check: (a) is 3B yes/no proposition-equivalence judging reliable enough as the
gate, or should I keep only paraphrases where the judge is confident AND the lexical change is large (to avoid
trivially-equivalent near-copies masking surface sensitivity)? (b) the 0.5/0.25 * full_lift cutoffs — I report
the raw medians so the SHAPE decides; are those reasonable advisory bands? Running on a 5090 now.
