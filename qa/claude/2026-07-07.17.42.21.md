# R40-s3 RESULT — surprise PASSES categorically (on/off-manifold), NULL as continuous bits law

Logs `docs/cloud_results/r40s3_{squad,synth}.{json,perqa.json,log}`, code `s3/wikibridge.py`. Pod reclaimed,
pushed 4a1ea9e. 1 seed (squad 2 streams / synth 4 streams — smoke). Old-only NON-replayed para EM:

| arm | squad | synth | gap |
|---|---|---|---|
| compact_cpt_qa_k0 | 0.76 | 0.08 | +0.68 |
| compact_cpt_qa_k0_noanchor | 0.60 | 0.187 | +0.41 |
| compact_cpt_qa_k1 | 0.65 | 0.10 | +0.55 |
| full replay (rep items) | 0.88 | 0.52 | +0.36 |

## Your pass criteria — read
- **PRIMARY (categorical source): PASS, big.** squad k0 0.76 vs synth 0.08 = +0.68, well past your +0.25
  scale bar. The KG null was a venue artifact confirmed. And the surprise contrast is REAL and survives your
  answer-length guardrail: **bits/token = 4.45 (squad) vs 11.37 (synth)** — off-manifold source is ~2.5x
  higher per-token surprise AND the one that collapses. Total bits are length-confounded (squad 4.9tok/19.3b,
  synth 3.9tok/44.5b); bits/token cleanly separates.
- **SECONDARY (within-source continuous bits): NULL/weak.** squad k0 corr −0.05 (total) / −0.26 (bits/token);
  synth k0 −0.10 / −0.13; k0_noanchor ~0. Right sign, within noise at n=25–75. So it's your "partial pass":
  the effect is CATEGORICAL (manifold membership), not a smooth per-item bits law.
- Full replay narrows but does not close synth (0.52 rep) — deficit is retention/write budget for off-
  manifold items, not total unlearnability.

## My read (want your check)
The surprise reframe is CONFIRMED but reclassified: retention cost = **O(#off-manifold exceptions)**, measured
cleanly by bits/token or source, NOT a continuous bits law and NOT a dissolution of the exception wall. This
directly founds Phase-1: gate replay/residual budget by on/off-manifold (spend only on high-bits/token items).

Two questions:
1. Caveat: squad got only 2 streams (old = stream 0 only) vs synth 4 — different stream counts (your guardrail
   #2). The floor gap is huge so I don't think it's an artifact, but do you want a SEEDS=2 + matched-stream
   confirm before I call it, or is +0.68 with the bits/token contrast enough to bank and move to Phase-1?
2. Phase-1 design: the cleanest test of "surprise-gated residual write" — I'm thinking an arm that computes
   per-fact base bits/token at commit time and only replays/writes-residual for items above a threshold
   (off-manifold), vs uniform k-replay at matched TOTAL budget. Headline: does budget-matched surprise-gated
   replay beat uniform replay on old-nonrep retention (i.e. is the budget better spent on exceptions)? Is that
   the right Phase-1, or do you want the s2 KG version first?
