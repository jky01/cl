# Rung 1 implemented (review gate) — surprise-gated replay budget on mixed squad+synth

Built to your last design; `s3/wikibridge.py` only; compiles clean. Launching after your read.

Implementation:
- `build_mixed`: each stream = that index's squad articles + synth articles; tags `q['src']` = squad|synth so
  the mixed stream contains both regimes at matched age + shared budget pressure (your key requirement).
- `select_budget(items, mode, B, seed)`: **surprise** = top-B old committed by frozen-base bits/token;
  **random** = B random (age-matched, deterministic); **lowbits** = bottom-B; **sourceoracle** = synth-first
  (diagnostic upper bound). B = round(WB_BUDGET_FRAC * len(old-committed-so-far)), recomputed each stream.
- Arms (auto-routed through run_ingest): `compact_cpt_qa` (full ceiling), `compact_cpt_qa_k0` (zero floor),
  `compact_cpt_qa_bgt_surprise`, `_bgt_random`, `_bgt_lowbits`, `_bgt_sourceoracle`. Matched budget across the
  4 bgt arms via WB_BUDGET_FRAC.
- rep flag for the old-only split = membership in the FINAL consolidation pool (one caveat below).
- high-bits/low-bits/all-old splits + exception-tail density (min replay frac to within 0.05 of full) computed
  OFFLINE from `.perqa.json` (join final_eval_em with base bits/token).

Two things I want your read on before the pod:
1. **rep-flag definition.** For budget arms an item's replayed-ness is defined as membership in the FINAL
   consolidation's pool (over ALL old items). An item replayed at an earlier stream but dropped at the final
   selection reads as non-replayed. I chose final-pool because final consolidation dominates final retention.
   Acceptable, or do you want "ever-replayed" instead?
2. **Run config.** Planning `WB_SOURCE=mixed WB_STREAMS=3 WB_SYNTH_OVERSEL=12 WB_BUDGET_FRAC=0.5 WB_SEEDS=1`
   as the first SMOKE (mechanism: does surprise_gate beat random at matched budget?), scale to SEEDS=2 if
   alive. Arms as above. Headline (offline): surprise_gate vs random_budget all-old & high-bits old para EM at
   matched budget; lowbits as negative control; exception-tail density. OK, or change budget_frac / streams?

If no blocker I launch the SEEDS=1 smoke.
