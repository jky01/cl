# R40 RESULT — merge retired (clean negative), surprise NULL on KG (wrong venue). Next: surprise on s3.

Logs: `docs/cloud_results/r40_smoke.{json,perfact.json,log}`, code `s2/lifecycle_bakeoff.py`
(run_mergeparallel/surprise_probe/surprise_summary). Pod reclaimed, pushed 25288c6. KG counterfactuals,
3 streams x 20, 1 seed, STEPS=600. old_para_final:

| arm | old_para | newest | base-hop after | ret/surprise-bit | conflict |
|---|---|---|---|---|---|
| naive_fixed | 0.625 | 0.85 | 0.191 | 0.072 | — |
| nswrite | 0.975 | 0.85 | 0.215 | 0.096 | — |
| ours_tgt_answerid | 1.000 | 0.85 | 0.205 | 0.098 | — |
| loramerge | 0.500 | 0.85 | 0.135 | 0.063 | — |
| merge_sum | 0.250 | 0.25 | 0.101 | 0.022 | 0.245 |
| merge_ties | 0.225 | 0.20 | 0.146 | 0.021 | 0.244 |

## Merge gate: FAIL both bars — retired
merge_ties/merge_sum land BELOW even sequential loramerge (0.22-0.25 < 0.50), collapse newest (0.2 vs 0.85)
and base-hop, conflict 0.244 by r3. Your prediction was right: independent-fact per-stream LoRA task vectors
occupy conflicting q/v directions and collide destructively at merge — the O(#facts) wall in weight space.
"Parallel removes sequential drift" is false for independent facts; it's WORSE than sequential fold. I'm
retiring merge as a route (not even a useful baseline here). Agree, or do you want a lambda/rank/density
sweep first? My read: it's so far below nswrite that a sweep isn't worth a pod — the collapse is structural
(conflicting directions), not a hyperparameter.

## Surprise gate: NULL, but I think the VENUE is wrong (want your check)
On naive_fixed (the only arm with forgetting variance) base-para surprise vs retention corr = +0.15
(excl-already-correct +0.21; tercile lo/mid/hi = 0.65/0.60/0.85) — weakly WRONG sign, within noise at n=58.
My diagnosis: KG counterfactuals are UNIFORMLY high-surprise (mean 9.72 bits, only 2/60 already-base-correct).
There is NO low-surprise / prior-anchored population, so the R38B-A floor can't appear. The surprise
hypothesis is intrinsically an on-manifold(real) vs off-manifold(synth) CONTRAST — which KG counterfactuals
cannot create. So R40 doesn't refute #1; it says KG is the wrong venue.

Proposal: re-run the surprise instrumentation on `s3/wikibridge.py` with BOTH `WB_SOURCE=squad` (real, low-
surprise, prior-anchored) and `WB_SOURCE=synth` (independent, high-surprise) through the SAME ingest path
(the R38B-A control), logging per-fact base surprise + final para retention, and test whether base surprise
predicts the real-vs-synth retention gap. That's the venue where surprise SHOULD have signal if #1 is real.

Two questions:
1. Confirm merge retirement (or insist on one cheap sweep)?
2. For the s3 surprise run: is per-fact base-surprise the right predictor, or should I bucket by whether the
   base is already-correct / margin sign (categorical on/off-manifold) rather than continuous bits — since
   R40 hints the effect may be categorical (manifold membership) not a smooth bits gradient?

nswrite re-confirmed (0.975 ~ replay 1.0, most surprise-bit-efficient 0.096). Instrumentation is now reusable
for s3. RESEARCH_MAP updated (merge ❌, surprise re-venue).
