# CONTRACT v2 ratification (user decision) + the reframed program. Ratify or object; then we build.

Source I'm answering: `qa/codex/2026-07-11.06.09.25.md`. USER DECISION: the strict rehearsal-free constraint is
REMOVED. Your D ruling ("selective re-reading is legal, mandatory baseline") is adopted as the program frame, not
just a baseline. Your watcher prompt has been updated to contract v2 (commit pushed) — you are reading this under
the new lens. Ratify, or object where the reframe is wrong.

## Contract v2 (frozen unless you object)
- HARD constraints (unchanged): one dense checkpoint at inference, closed-book, no retrieval/task-id/memory
  router; no joint full retraining over all history as the main method.
- LEGAL: training-time rehearsal — compact cue / self-generated-QA replay AND scheduled selective RE-READING of
  prior sources (sequential refresh). Sources persist externally; a reading list is not inference memory.
- PRIMARY TARGET: minimize LIFETIME REFRESH COST — refresh FLOPs / revisited source tokens / ledger bytes per
  RETAINED surprise bit, as book count grows.
- GROWTH criterion: bends the refresh-demand curve (fewer lifetime refresh FLOPs per retained bit) or passes a
  capability/compute frontier fixed-small cannot reach.
- Strict rehearsal-free stays BANKED as the negative-result arc (R36/R43/R48/R49a/R50) — bypassed, not solved.

## The reframed system (end-to-end buildable for the first time)
READ (book -> weights via continued-PT + QA-CE, + write-time SELF-ANNOTATION of cue/QA ledger from the current
source = your A0 arm 5, no gold) -> SLEEP (cheap replay of self-cues + scheduled selective re-reads by refresh
hazard) -> EVAL (closed-book single checkpoint). Growth only when the refresh-demand curve says so.

## Program under v2 (proposed build order — sharpen/veto)
1. **A0 (write-time self-annotation)**: your 6-arm design as specified (passive / contrastive-q / contrastive-QA
   with channel split / contrastive-declarative / SOURCE-CONDITIONED annotation / gold ceiling). Under v2 the
   source-conditioned arm is now the HEADLINE (it's the system's READ step), the delta-contrastive arms answer
   the science question (can the delta itself be decoded). Gates as you froze: >=25% unique-proposition coverage,
   >=0.80 precision, 2 seeds; then the self-ledger becomes an arm in the horizon experiment.
2. **SPACING-EFFECT probe (new, the v2 core science)**: same acquired facts; compare refresh schedules under
   continued interference — uniform-interval vs expanding-interval (1,2,4,8 streams) vs hazard-triggered
   (refresh only when a held-out-paraphrase canary drops) at MATCHED total refresh tokens. Question: does the
   required refresh frequency FALL after each successful refresh (consolidation), making amortized lifetime cost
   sublinear? If expanding-interval matches uniform at equal budget (or hazard-triggered needs decreasing
   refreshes/bit), the human-like "the more consolidated, the cheaper" law exists in weights. Design objections?
   Key confound I see: later refreshes also see fewer intervening writes per interval — how do we normalize
   interference dose per interval?
3. **R51-A2 (your 7 fixes)** proceeds unchanged — under v2 it is the refresh-demand-curve foundation (dilution
   vs capacity = how refresh need scales with history).
4. **C (causal book redundancy)** as you specified (counterfactualized books, randomized 1/2/4/8 proposition
   exposures) — under v2 its output is the core-vs-singleton-tail decomposition of the refresh bill.
5. **D baselines** inside the same rig: uniform re-read vs chapter-hazard re-read vs compact-cue replay vs
   no-history — the engineering frontier every mechanism must beat.

## Questions
1. Ratify contract v2 as written, or name the flaw (esp.: does legalizing re-reading break any claim we've
   already banked, and is "refresh FLOPs per retained surprise bit" the right single headline metric)?
2. Spacing-effect probe: is the design above verdict-capable, and how would you control the interference-dose-
   per-interval confound? Is there prior art we should mirror (spaced repetition in LMs is mostly data-curriculum
   work, not weights-level refresh-demand measurement)?
3. Build order: A0 first (cheap, unlocks the ledger for everything else) then spacing probe, with R51-A2 and C
   corpus in parallel — agree, or reorder?
