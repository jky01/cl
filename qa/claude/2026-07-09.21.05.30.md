# R44 RESULT: NULL for relation-specific schema compression; real ANSWER-KIND effect + cross-kind interference

Artifacts: `docs/cloud_results/r44_ladder.{json,log}`, `s3/schema_comp.py`. Full ladder ran: 6 real
T-REx relations, 2 seeds, N∈{0,4,8,16}, B∈{2,4,6,8}, 3 arms, manual-para primary eval. FINDINGS R44
pushed. Pilot calibrated B window to 2–8. Pod reclaimed. Your two blocking fixes applied
(base-anchor detach; full-run diagnostics).

## Headline (mean target manual-para EM at B=6, mid-curve)
| bucket | floor N=0 | schema_commit N=16 | item_k_matched N=16 | shuffle(deranged) N=16 |
|---|---|---|---|---|
| person (n=8) | 0.438 | 0.891 | 0.828 | **0.906** |
| org (n=4) | 0.719 | 0.969 | **0.562** | 0.875 |

## Against your pre-registered gate → FAIL for the relation-schema claim
- **person: schema_commit ≈ item_k_matched ≈ shuffle.** schema_commit − item_k_matched flips sign across
  N (B6: +0.219, −0.11, +0.063 for N=4/8/16), mean ≈0. Shuffle even highest. No relation-specific slope.
- **org: schema_commit > item_k_matched (Δ≈+0.31–0.41 at N=16) BUT schema_commit ≈ shuffle** (shuffle
  0.875 vs schema 0.969, close; both >> item 0.562). Your criterion: deranged same-kind passing ⇒ the
  effect is answer-KIND/template support, not relation schema. So the relation-specific hypothesis FAILS.

## But it's not a pure null — three real data:
1. **Priming helps binding** (person floor 0.44 → any primed arm ~0.88 at B=6): consolidating facts first
   roughly doubles low-budget binding EM.
2. **Reusable structure is answer-KIND, not relation-specific**: same-kind-different-relation (shuffle)
   ≈ same-relation (schema_commit).
3. **Cross-kind consolidation INTERFERES**: org `item_k_matched` (0.562) < floor (0.719). Mixing
   person-name + org-name + other-kind facts in one consolidation HURTS org-target binding. Clean
   mechanistic datum linking to the R43 interference story — the write path shares an answer-TYPE schema,
   and mixing incompatible types is destructive.
Prime retention post-bind 0.77–0.97 (no catastrophic prime forgetting).

## My read + questions
This lands us in the "honest negative map" branch you pre-registered (if R44 fails cleanly → reframe,
don't keep claiming the current path solves the full frontier). But finding (3) is a genuinely new,
mechanistically-clean positive: **answer-KIND is a real reusable schema unit, and cross-kind mixing
interferes** — that's an actionable lever the R43 unimodal-null did not give us.

1. **Do you accept the R44 null for relation-schema, and agree the answer-KIND interference finding is
   the salvageable signal?** Or is org's n=4 too thin — do you want a confirm run with more org relations
   (T-REx has developer/manufacturer + I could add publisher P123, director-vs-... to widen org) and
   maybe a 3rd answer-kind bucket, before finalizing?
2. **Given answer-KIND is the reusable unit, is the honest next move to STOP chasing schema and write the
   capstone negative map** (R33/R38 engineering win + R40/R42/R43/R44 precise frontier boundary:
   surprise=risk-not-router, real text unimodal, schema compression is answer-kind-coarse not
   relation-fine, cross-kind interferes)? That is a real, publishable contribution even if it is not "CL
   solved".
3. **Or is finding (3) worth ONE more targeted shot** — e.g. "kind-segregated consolidation": route
   facts to consolidation batches by answer-kind so cross-kind interference is avoided, testing whether
   that recovers a compute advantage over mixed replay (a concrete, cheap mechanism the interference
   datum motivates)? This would be the last frontier probe before the capstone.

My lean: report the negative map honestly (bank R33/R38 + the precise boundary), and offer finding (3)'s
kind-segregated consolidation as ONE optional cheap probe — but I do not want to keep drilling a wall the
evidence says is answer-kind-coarse. Push back if you see more signal in R44 than I do.
