# R37-A: clean decoy CONFIRMS the signal (leak wasn't driving it) — full 2-seed launched

Fixed both your bugs (strict-disjoint decoy: exclude ALL used subject names, KG-aware; `footprint_diag`
now in `RESULT_JSON`). Pushed 883969f. Re-smoked the CLEAN decoy (3 streams so an old stream exists):

| arm (3×20/120, 1 seed) | all-seen | oldest forget | delta_old_final | delta_decoy_final |
|---|---|---|---|---|
| naive | 0.317 | +0.300 | — | — |
| grow_local_anchor | 0.317 | +0.300 | **0.046** | 0.045 |
| **grow_local_decoy** | **0.533** | **+0.150** | **0.014** | 0.006 |
| grow_local_oracle | 0.217 | +0.500 | 0.011 | — |

**The signal survives the fix — it was NOT the leak.** Strict-disjoint same-template unlabeled decoys
still beat naive (+0.22 all-seen, forget halved) and drive `delta_old` 0.046→0.014. Mechanism is clean:
- **anchor**: `delta_anchor`→0 but `delta_old ≈ delta_decoy ≈ 0.045` — the block fires on *all*
  fact-shaped prompts (old included) because anchors don't cover fact-shape → = naive (your predicted
  template-inseparability failure, now measured).
- **decoy**: penalizing `delta` on disjoint-subject fact-shaped prompts **generalizes to old-stream
  prompts** (`delta_old`→0.014), because "be quiet on non-new-subject fact prompts" transfers to old
  subjects. Retention improves, newest unchanged (0.6) → plasticity intact. This is the first no-replay /
  no-router / no-old-label mechanism to **materially beat naive on same-template data** — clears your
  minimum-viable gate (+0.10 all-seen, −0.15 forget).

**Surprise to flag: `oracle` is WORSE than naive** (0.217, forget +0.500) despite the lowest `delta_old`
(0.011). Counterintuitive — penalizing footprint on the *actual* old prompts drives the block quiet on
them yet old recall collapses. My hypotheses: (a) 1-seed/3-stream noise; (b) penalizing the exact old
prompts every step over-constrains the block into a solution that disrupts the *frozen-layer* read path
for old facts (the block being forced to identity on old inputs isn't the same as preserving the
end-to-end old answer through the whole stack). The 2-seed run will tell us if it's real. If real, it's
an interesting result: footprint-identity on old inputs ≠ answer-preservation (echoing R36-C's
"input-direction protection ≠ answer protection").

**Launched the full 2-seed pilot** (naive, ours_tgt_answerid, grow_local_{anchor,decoy,oracle}, 6×40,
λ=1) — reading against your gates. If `grow_local_decoy` holds at 2-seed, I'll then run the **growth
controls** you required before any growth claim: `BK_LOCAL_NOGROW=1` (fixed-capacity + same locality
loss) and a large-from-start identity stack — to check whether the retention comes from *growth* or just
from the locality loss on fixed capacity (R23/R31 suggest the latter; this would test it directly for the
localized-write mechanism).

Two questions:
1. On the oracle paradox — if it holds at 2-seed, do you read it as "footprint-identity ≠ answer-
   preservation" (a real sub-finding), or a sign my locality metric (hidden Δ norm) is the wrong causal
   quantity and I should try the answer-logit-drift localizer you mentioned as the bridge experiment?
2. If `grow_local_decoy` clears the *project-relevant* bar (approach nswrite ~0.65, beat NOGROW control),
   is that a genuine R37 positive worth its own FINDINGS entry — "no-replay retention via localized-write
   growth with same-template unlabeled coverage" — or do you still see a hidden contract violation (the
   decoy distribution is a form of unlabeled data coverage — is that within the no-inference-memory /
   no-old-replay contract, given decoys are generated, not stored old prompts)?
