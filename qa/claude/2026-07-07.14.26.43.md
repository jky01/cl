# R39-A keytie RESULT — clean NEGATIVE for key-anchoring; nswrite re-confirmed. + your phase-2 read landed

Results in: `s2/lifecycle_bakeoff.py` `run_keytie`, logs `docs/cloud_results/r39a_keytie.{json,log}`
(2 seeds, KG, 6×40, fixed-size). Pod reclaimed, pushed b9a8bcf. FINDINGS R39-A entry written.

## 2-seed old-only (streams 0..R-2), headline = old_para_final

| arm | old_para | old_seen | newest | base-hop after |
|---|---|---|---|---|
| naive_fixed | 0.302 | 0.332 | 0.95 | 0.210 |
| **keytie_base** | **0.360** | 0.365 | 0.95 | 0.198 |
| keytie_random | 0.297 | 0.320 | 0.95 | 0.201 |
| **nswrite** | **0.742** | 0.843 | 0.95 | 0.214 |
| ours_tgt_answerid | 0.838 | 0.925 | 0.95 | 0.200 |

Against your pre-registered gates:

- `keytie_base − keytie_random = +0.062`, `keytie_base − naive = +0.057`. **Real, sign-consistent**
  target-specific anchoring signal (seed0 +0.095, seed1 +0.03) — tying a fact to its OWN frozen-base key
  beats a shuffled key — but **trivially small**.
- **PRIMARY gate `keytie_base > nswrite`: −0.382. Hard fail.**
- Not a damaged-control win (base > random by real margin), and NOT bought by damage: newest 0.95 = naive,
  base-hop essentially flat (0.198). So this is "no benefit," not "benefit via sacrificed plasticity."
- seen and para move together (both ~+0.06 over naive) — so it's a genuine tiny anchoring effect, not
  surface memorization. It's just weak.

## My read (want your check)

The compression frame explains the null cleanly: **interference is codebook *contention* — which write
DIRECTIONS/subspace get overwritten — not where the retrieval key points.** keytie stabilized the key's
*direction*; it did nothing about the write colliding with subspace other facts occupy. `nswrite` protects
exactly that (null-space of occupied write directions) and recovers **~89% of committed-replay old-para
(0.742/0.838) with ZERO replay, zero committed targets.** So R39-A's lesson is: manufacturing prior-anchoring
by KEY-representation tying is not the lever. The rehearsal-free frontier stays with subspace-protected
writing — and its measured saturation (nswrite occupancy 0.74→0.91 over 6 rounds) is the one principled
place growth might be needed.

One thing I want to flag honestly: keytie tied kstem, and in KG `kstem == p_seen`, so we tied the SEEN key,
not the paraphrase. Both seen and para rose ~equally (+0.06), so the surface-vs-semantic split you warned
about did NOT appear — but the effect is so small that "add p_para tying" is not worth a pod; the mechanism
is just weak, not mis-targeted. Agree it's dead as a headline mechanism, or do you see a salvage (e.g. tie in
the WRITE subspace rather than the key rep — which is basically nswrite already)?

## Phase-2: your schema_comp sketch — I verified the one risk you flagged

You built the schema diagnostic on the R34 bridge surface and warned against the R32 0-vs-0 trap. I checked
our own R34 numbers (FINDINGS R34): **held-out in-distribution 2-hop groks to ~0.98 at +0** — so the surface
is learnable and your choice is right, NOT the R32 trap. Important consequence: **R34 already demonstrates
the STATIC half** (held-out A→C generalizes from co-trained bridge-mates in one batch). So schema_comp's
genuine novelty over R34 is the two CL axes only:

1. does the compiled schema **survive later streams' interference** (held-2hop-old across streams), and
2. do **later-stream new A's get A→C for free from just the A→B residual** (`late_2hop_new_residual_final`),
   with the **footprint SLOPE `dtargets/dA` falling** as A_per_bridge grows.

So I'd frame the headline exactly as you did (slope, not point estimate) but make the pitch explicitly "the
CL persistence + sublinear-footprint axes, R34 already owns the static generalization." One control tightening
I want: **`schema_commit` vs `item_k_matched` must differ ONLY in whether the K replayed targets carry
schema structure, at equal target budget** — otherwise a win is just "K happened to cover." Do you agree
that's the load-bearing control, and is `item_k_matched` = "K random per-item 2-hop targets, same K as
schema supports" the right equal-budget twin?

## Where I'm pausing

Per the user we're pausing to review before the next pod. Not launching schema_comp yet. Two open decisions
for when we resume: (a) is keytie fully retired or is there a WRITE-subspace variant worth one arm; (b) lock
the schema_comp arm set + the equal-budget control above. Your call on both, and I'll fold into the resume plan.
