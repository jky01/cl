---
name: s0-step0-state
description: "Status, results, and hard-won debugging findings for the s0/ Step-0 capsule-memory experiment"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9c19101e-fabc-4e34-a5e4-fb73c5c5e9ed
---

`s0/` implements Step 0 of the neuromodulated continual-learning architecture (spec in `reference/3.md` §11.4): an amortized, no-gradient fact-capsule memory around a FROZEN proxy transformer. Goal of Step 0 = store ONE new (subject,relation,object) fact and read it back. Runs on CPU via `.venv/bin/python -m s0.run [--smoke]`.

**RESULT (2026-06-30, SOLVED): Step-0 gate fully passes.** N_facts=1 capsule exact-match **1.000 + locality 1.000** at smoke scale; **0.969 / 0.984** at d=128/60-vocab. Multi-fact emerged for free (nf=4 ≈0.52, nf=16 ≈0.32, locality stays ~0.92–1.0). The fix that did it: **token-level (subject,relation) retrieval keys + cross-batch InfoNCE** (see the contrastive-collapse story below). Before this, storage worked but locality was stuck ~0.1–0.6 for ~12 experiments.

Key debugging findings (each cost a long CPU run to establish; don't re-derive):
- **Write path failed only due to LR.** At lr=2e-3 the attention-pool FactEncoder + WriteNet stack diverges instantly and sits at chance; at **lr=1e-3 + grad-clip 1.0** it reaches 0.96. Earlier "object not in v" probes were this divergence, not a capacity limit. Added a `z_f→v` residual skip in `WriteNet` as insurance.
- **Read/inject path is sound** — answer-loss-only reaches acc 1.0 at N_facts=1; the gate opens itself (no deadlock).
- **Mixing N_facts=1..4 during training COLLAPSES even N_facts=1** (unlearned multi-slot retrieval poisons the shared read path). So Step 0 trains `max_facts=1`; multi-fact retrieval is Step 1. `run.py` default/smoke now set max_facts=1.
- **Locality mechanism**: injection gate `g = g_content(H_ans,R) · relevance(conf)`, where conf = top key-match score and relevance = σ(20·(conf−thr)) with FIXED steep slope + LEARNABLE threshold. A `margin` loss (matched query score >0.7, unrelated <0.1, IN-EPISODE negative only — cross-batch negatives collapse) separates conf. Locality metric compares the read vs the no-injection read `lm_head(inject_ln(H_ans))` (NOT raw core — that double-counts the inject_ln).
- **LOCALITY ROOT CAUSE FOUND (GPU session 2026-06-30): retrieval contrastive collapse.** Scaling the core did NOT help (it made locality worse: a better core makes the <ans> hidden MORE subject-invariant by the random-binding design). Decisive isolation results:
  - The learned-query **attention-pool encoders (FactEncoder/QueryEncoder) COLLAPSE under contrastive** (matched & random query·key identical; retr@1 stuck at chance for both hinge AND 128-neg InfoNCE). They train fine under supervised CE (so storage/v path is OK) but not contrastive.
  - **Subject identity is washed out** of pooled query hidden (linear probe: subject 0.11, relation 0.67) — mean-pool dilutes it.
  - **FIX (validated, retr@1=0.914):** build retrieval key/query from the SUBJECT and RELATION **token hidden states**, gathered by token-id range (see `s0/diag_tokret.py`), + InfoNCE. Mean-pool InfoNCE only reached 0.41; token-gather hits 0.91.
- **DONE — the rebuild worked.** `capsule.py` now has `gather_sr()` + `SRKeyEncoder`; `CapsuleMemory.key_enc`/`query_enc` build k_sem (statement S,R tokens) and q_sem (query S,R tokens); `WriteNet` no longer makes k_sem (owns v/addr/aux only); `train.py` uses cross-batch InfoNCE (`retrieve` λ=1.0), margin loss removed. Relevance gate (fixed slope 20 + learnable thr) + warmup kept. Result above.
- **STEP 1 (multi-fact) now also works** via a CURRICULUM that ramps episode size 1->max_facts over the first 70% of training (in `train_omega0`). Without it, training directly on max_facts=16 collapses to chance (ans loss never gets traction). With it, d=128/60-vocab/omega8000: nf=1 **1.00**, nf=4 0.98, nf=8 0.94, nf=16 0.87, nf=32 0.76 (extrapolates past training), locality 0.94–1.0 throughout. run.py defaults now max_facts=16/omega8000.
- **STEP 2 (conflict versioning, §27.10) DONE.** Non-destructive update: write a conflicting (s,r) as o_before@time=0 then o_now@time=1; a now/before query routes to the right version. Mechanism: slot gains a `time` field (SlotLayout.d_time); read derives `c_target` from the query's now/before token via `ctx_enc`, biases retrieval toward the slot whose time matches (`ver_weight=0.5`), with a sharp read softmax (`read_temp=0.1`) so the selected version isn't blended. `train_omega0` has a `_conflict_loss` sub-batch each step (lam conflict=1.0). Result (smoke): now-acc 1.00, before-acc 0.97, routing-fail 0.00, and no regression (nf=1 1.0/1.0, nf=16 0.76). `eval_conflict` in train.py, reported by run.py. World gained now/before tokens + `render_query_versioned` + `sample_conflict_episode`.
- **STEP 3A (capacity / occupancy allocation) DONE.** Diagnosed that the recall drop under load was OVERWRITES, not retrieval: `rec|surv` is flat ~0.87 regardless of N, recall ≈ survival×0.87, and the content-addressed product-key allocator collided (birthday) even below capacity. Fix: `CapsuleMemory.write` now places into a FREE slot (read scans all slots by k_sem, so slot index is free to choose; no differentiable loss depends on it), with FIFO (oldest-time) eviction when full. Result: survival=1.000 for N≤n_mem, and it ALSO fixed multi-fact (nf=16 0.76→1.000, since within-episode collisions are gone). Over capacity, survival=n_mem/N (FIFO). The product-key allocator + balance loss are now vestigial (balance λ=0).
- **STEP 4 (commit gate / validate-and-rollback, §27.18/§27.21.5) DONE.** Admission control: `write(trust=...)` computes `g_commit = sigmoid(commit_net([trust, conflict]))` where conflict = max k_sem sim to an occupied slot; g_commit is stored as the slot's `alloc` (soft presence), and read replaced the hard alloc mask with a `commit_weight*log(alloc)` bias (alloc~1 → 0, so trusted writes unchanged; alloc~0 empty/rejected → suppressed). `_safety_loss` (lam safety=1.0): write reliable (trust=1) then a CONFLICTING unreliable (trust=0) update; the reliable value must still be recalled, plus direct BCE so g_commit→1 for reliable, →0 for unreliable. Result: reliable-recall-after-attack 1.00, admission g reliable 1.00 / unreliable 0.00, no regression. Gate generalizes: trust drives admission; trust=1 conflicting writes are still admitted (legit versioned updates, Step 2 still 1.0/1.0).
- **STEP 5 (lifelong forgetting, the headline claim) DONE.** `eval_lifelong`: learn 8 sessions × 6 facts in order, recall EACH session at the end. Capsule = **1.00 flat across all session ages (zero catastrophic forgetting)**; the sequential-LoRA rival (`_seq_lora_recall`, one adapter fine-tuned session-by-session) fails (~0.02). Fixing this caught a latent bug: version routing was applied to PLAIN queries (default c_target=1 penalised old-time slots below the relevance threshold → false forgetting); now the version bias is gated by `has_ctx` (only when the query carries now/before). NB the smoke d=64 core is too weak for LoRA to show a clean decay-with-age curve (it fails uniformly); the contrast is cleaner on the bigger model.
- **s0 now demonstrates the full thesis**: single+multi-fact write/read, locality, conflict versioning, capacity (zero within-bound forgetting), trust-gated admission, and lifelong zero-forgetting vs a parametric rival. ~5 commits (473b941 → ...).
- **GROWTH operator (`core.grow_deeper`) added + de-risked** — toward the user's "small model → continually grow into a big model" goal. Appends identity-initialised blocks (attn out-proj + mlp final linear zeroed → block(x)=x): mid-run L=2→4 gave max|Δhidden|=**0.00e+00**, recall delta 0.0000 (zero-forgetting growth); width unchanged so memory modules' interface intact; after unfreezing new layers + continued training recall holds ~0.97. PROVES "growth doesn't hurt"; does NOT yet prove "growth helps" (needs a task that saturates the small core). Deepening chosen over widening because widening changes d_model and breaks every module that reads hidden states.
- **GROWTH IMPROVES CAPABILITY — validated** (`s0/diag_grow_hops.py`). Depth-sensitive task = in-context K-hop chain traversal (shuffled edges, follow r to chain end; each hop ~needs a layer). L=2 saturates (K2 0.52, K3 0.33). grow_deeper L=2→4 + continued training breaks through (K2 0.98, K3 0.73, K4 0.42) while K1 stays 1.00 (no forgetting). The decisive control: L=2 trained for 2× steps stays plateaued (0.53/0.34) → the gain is from added DEPTH, not more compute. So both halves of the small→large thesis hold: growth doesn't forget (hidden Δ=0.00) AND growth adds capability that in-place training can't.
- **AUTONOMOUS small→large loop — validated** (`s0/diag_autogrow.py`). A plateau-triggered controller (loss barely improving while high-K still failing = capacity bottleneck → `grow_deeper(2)`, rebuild optimizer) grows the core BY ITSELF from L=2→4→6 on the K-hop task. K2 climbs 0.46 (fixed-L=2, stuck even after 16 chunks) → 0.96 (auto-grown); K1 stays 1.00 (no forgetting). Honest bound: K3+ stays low even at L=6 (K3 0.45, K4 0.30) — harder hops need still more depth/training; the closed LOOP is proven, the hard task isn't fully solved. Trigger is a heuristic (placeholder for §27 SleepGate/CapacityNet learned gate).
- **The small→large thesis is now demonstrated end-to-end in sandbox:** continual learning w/o forgetting (memory), function-preserving growth w/o forgetting, growth adds capability in-place training can't, and an autonomous saturation→growth controller. 9 commits.
- **NEURAL growth trigger — validated with honest caveats** (`core.GrowableCore`, `s0/diag_neurogrow.py`). Replaced the heuristic plateau trigger with learned per-layer gates: n_base always-on blocks + n_grow GatedBlocks behind a sigmoid gate (init ~0 = identity); loss = task CE + λ·grow_gate_penalty. The gate is cost-sensitive: λ=0.5 → gates exactly 0, capability = true L=2 (K3 0.31); λ=0.02 → capability K3 0.97. So the trigger is now fully differentiable/loss-driven, no hand threshold. CAVEATS (point to the clean fix): (1) L1-on-gate is GAMEABLE — the net keeps the gate scalar ~0.01 but up-scales block weights, so the gate value under-reports usage (must measure usage by ABLATION, not gate value). (2) Ablation showed the grow blocks only net-add ~0.12 at K3 (0.96→0.84) while base-2 alone reached 0.84 (vs 0.33 standalone) — i.e. part of the gain is overparameterized-optimization easing, not inference-time depth. Clean next: penalize the gated-contribution NORM (ungameable) instead of the gate scalar.
- **Norm-cost attempt — done, honest MIXED result.** `grow_gate_penalty` now returns the summed RELATIVE residual contribution of the grow blocks (`_last_grow_cost` from hidden()), which is ungameable. Outcome: the λ=0.5 run FULLY SOLVED K-hop (K5 0.97 vs L=2's 0.18) and ablation confirms the grow blocks are essential (K3 0.99→0.46) at tiny contribution norm (~0.01) → growth delivers capability efficiently and ablation is a faithful usage check. BUT the cost magnitude is NOT a clean monotonic "how-deep" dial: the smaller-λ (0.05) run did WORSE (counter-intuitive; single-seed, likely optimisation variance), and a tiny-norm contribution can still be essential. CONCLUSION: the neural growth MECHANISM + ablation-faithful verification hold; a clean cost-controlled growth DIAL remains OPEN (needs multi-seed / scheduling / a different controller — a real research point, not a tuning fix).
- **NEXT options (productization / depth):** clean neural growth dial (multi-seed/scheduled controller); expert/MoE growth (§27.11) + distill-into-core (§27.12); the real-model path (Qwen): free-text key encoder (replace `gather_sr` token-id trick) + generative KV/prefix value injection + compete vs RAG; §28 end-to-end Ω meta-training.

GPU is set up: torch **2.11.0+cu128** in `.venv` (2.12.1 had no CUDA wheel), RTX 2070. `run.py` auto-selects cuda; `mem.to(device)` added; defaults now reproduce the pass (d=128, 60-vocab, core 1500, omega 4000). The ~10 `s0/diag_*.py` investigation scripts were removed after use (findings captured here). See [[s0-step0-design-constraints]].

## Qwen real-model path (new phase, 2026-06-30)
- transformers 5.12.1 + Qwen2.5-0.5B in `.venv` (fp16, ~1GB on the RTX 2070). `s0/qwen_iface.py`, `s0/qwen_retrieval.py`.
- **Injection interface verified**: `max|lm_head(final_hidden) - logits| = 0.00` → same hook as the proxy (h_last + g*R → renorm → lm_head). Arch: hidden=896, 24 layers, GQA(14/2), vocab=151936, tied emb, Qwen2RMSNorm.
- **Blocker #1 (free-text key) CRACKED**: Qwen frozen mean-pooled last-hidden + small trained projection + InfoNCE → **retrieval@1 = 1.000** on free-text paraphrased facts (toy proxy mean-pool was 0.41). Only the projection trains (no backprop through Qwen). The template-bound `gather_sr` is replaceable.
- **NEXT (Qwen):** (b) generative value injection (KV/prefix across generation); (c) full free-text write/read on Qwen; (d) benchmark vs RAG + vs LoRA.
- **Qwen full write/read WORKS** (`qwen_memory.py`): free-text fact → key+value from frozen Qwen features; cloze query retrieves + injects `H_ans + g*R → lm_head` (single-token answers; direct additive injection, NO inject_ln — tied lm_head makes R≈emb[answer] a clean logit boost). Capsule recall NO-context **0.961** vs no-mem 0.047. Needed lr 5e-4 + grad-clip (was 0.97/0.05 across runs). Data MUST use sensible typed values per attribute (random pairing → nonsensical facts even RAG fights).
- **Qwen vs RAG, HONEST**: fair few-shot RAG = 0.945 (zero-shot cloze RAG 0.195 only because Qwen-0.5B-base copies unreliably in cloze — model limitation, verified `qwen_ragcheck.py`, not a bug). Accuracy COMPARABLE → memory does NOT beat RAG on accuracy; its value = no-context-cost + the §27 features (versioning/admission/capacity/lifelong) raw RAG lacks. NEXT: multi-token/KV-prefix injection; port §27 features onto Qwen and benchmark THOSE vs RAG; scale + multi-seed.
- **MEMORY BEATS RAG on Qwen — temporal versioning** (`qwen_conflict.py`). Each fact 2 versions (v1@t=0, v2@t=1, sharing a key from (name,attr) only); Currently/Originally query routes by explicit time. **Memory current 0.984 / original 0.992; raw-text RAG (both statements shuffled, undated, in context) ~0.22 both.** Needed explicit retrieval InfoNCE + DIRECT c_target supervision (current→1/original→0) or ctx_enc collapses (same fix as the proxy relevance gate). This is the genuine §27-feature win over RAG on a real LM. Caveat: beats UNDATED text-RAG; a timestamp-in-text RAG could too, but at context cost — which IS the structured-memory advantage.
- **Blocker #2 (multi-token answers) CRACKED** (`qwen_multitoken.py`): per-step injection (inject retrieved R at EVERY answer position) recalls multi-token free-form answers ("San Francisco","software engineer") with NO context. Free-generation exact-match (both tokens) **0.979** vs no-mem 0.000 (few-shot RAG 1.000 = parity). REQUIRED left padding (right-align real tokens → last hidden `[:,-1]`, appended gens at sequence end); a right-pad bug silently capped accuracy (~0.7→0.98). Both real-model blockers (#1 free-text key, #2 multi-token value) now down. Qwen memory handles realistic free-text multi-token facts. NEXT: §27 features (admission/capacity/lifelong) on Qwen vs RAG; scale + ANN; 3+ token / KV-prefix; multi-seed.
- **2nd structural win vs RAG on Qwen — ADMISSION/trust** (`qwen_admission.py`, §27.18): reliable fact + untrustworthy contradiction; commit gate (g_commit=σ(commit_net([trust,conflict])) stored as soft slot presence, read suppresses via log(adm)) admits reliable 1.00 / rejects untrust 0.00 (direct BCE). **Memory reliable-recall 1.000 vs naive text-RAG (both unmarked facts in context) 0.188.** Ported from proxy first-try. So two clean RAG-beating structural features on Qwen: temporal versioning (0.98 vs 0.22) + admission (1.00 vs 0.19). NEXT: lifelong vs sequential-LoRA on Qwen (needs backprop-through-Qwen); capacity/eviction; scale+ANN; multi-seed.
- **GROWTH PENALTY — make-or-break de-risk (`diag_growpenalty.py`).** Grow 2→4→6→8 (train at each stage) vs from-scratch at each size, matched steps, 3 seeds. Mean avg-acc gap (scratch−grown) at L=2/4/6/8 = 0.00/−0.04/+0.04/−0.07 — SMALL and NOT widening with depth; at L=8 grown is slightly better (0.72 vs 0.65). Huge per-seed variance (L8 scratch 0.46–0.90, grown 0.55–0.93). KEY: **no compounding growth penalty at toy scale, and growth is MORE STABLE** (steady climb every seed) than from-scratch-deep (a gamble that sometimes fails to optimize). De-risks "small→large via growth"; caveats: toy scale (d=128, 2→8L), 1 task, N=3 — "super-large" unproven, lit's large-scale growth-penalty concerns stand. NEXT toward the goal: push growth further/larger (more layers, bigger d, more seeds) to find where (if) the penalty emerges; eventually combine growth + memory + real model.
- **GROWTH PENALTY at LARGER scale (`diag_growpenalty2.py`, 2→12 layers, kmax=7, 2 seeds).** Mean gap (scratch−grown): L4 +0.04, L8 **−0.29**, L12 **−0.15** → growth BEATS from-scratch at depth (no penalty, opposite). seed0 grown climbs 0.36→0.51→0.61→0.92→0.98→0.99 (depth-curriculum / warm-start cracks harder hops), while from-scratch deep is erratic (scratch L8=0.36 failed to optimize). seed1 a hard seed (both ~0.4). CONCLUSION: the "compounding penalty" fear is refuted more strongly at larger depth — growth is a MORE RELIABLE path to a working deep model (gradual-stacking benefit), no compounding degradation. Caveats: toy scale, 2 seeds, high variance; from-scratch instability is partly small-width/limited-steps, so the win margin likely shrinks at real scale. Two positive de-risks of small→large now. NEXT toward goal: bigger still / width growth / more seeds; or combine growth+memory+real model.
- **GROWTH + MEMORY COMPOSE (`diag_growmem.py`)** — the critical integration check for "continually learn AND grow". Proxy, single-fact memory: R0 base recall 0.953; R1 after function-preserving growth (L2→4, identity frozen) 0.926 (≈R0, preserved); R2 after the new layers TRAIN (core hidden drifts) 0.179 (memory keyed on old hidden breaks); R3 after a cheap memory re-adapt (retrain small modules on the evolved frozen core) 0.999 (fully recovered). CONCLUSION: growth doesn't permanently destroy accumulated knowledge — preserved at the growth moment, and cheaply re-synced after the core evolves. Honest mechanism: "no forgetting" = facts are re-encodable/re-presentable + cheap module re-adapt, not bank-untouched. All three pieces of small→large now validated AND shown composable: no-forget memory + penalty-free growth + growth-preserves-memory.
- **END-TO-END "learn AND grow" loop (`diag_integrated.py`)** — the small→large vision composed into ONE running system. Over L=2→4→6: core capability C (K-hop avg) rises 0.009→0.522→0.610 (grows deeper + trains), while fact-recall R (memory) stays 0.962→0.992→0.999 across every growth (cheap re-sync each phase: freeze core, retrain memory modules). So a single system grows its core capability over time WHILE its memory keeps recalling facts without forgetting, across multiple growth events. All three pieces (no-forget memory + penalty-free growth + growth-preserves-memory) now demonstrated TOGETHER in one loop. Caveats unchanged: toy scale (d=128, ≤6L), single capability axis (K-hop), memory re-sync retrains modules (cheap not free), facts re-encoded per eval. The "prove the idea" phase is comprehensively done (22 commits); remaining = real-model (Qwen growth) + true large scale, both big new phases.
- **GROWTH TRANSFERS TO REAL QWEN (`qwen_grow.py`).** `grow_qwen` appends identity-initialised Qwen2 decoder layers (deep-copy a layer, zero self_attn.o_proj + mlp.down_proj so residual adds=0 → layer(x)=x; also fix new layer_idx + extend cfg.layer_types so the forward's per-layer attn-type lookup doesn't IndexError; use_cache=False). 24→28 layers: **max|Δlogits| = 0.000e+00**, next-token unchanged ("...France is"→" Paris" before & after). So function-preserving deepening is NOT toy-specific — it works on a real LM with RMSNorm/RoPE/GQA/SwiGLU. NEXT (heavier, needs backprop-through-Qwen): train the grown Qwen layers to add capability + show the Qwen memory composes (re-sync) — the real-model version of diag_integrated.
- **GROWTH + MEMORY COMPOSE ON REAL QWEN (`qwen_integrated.py`)** — diag_growmem reproduced on Qwen2.5-0.5B. R0 Qwen memory recall 1.000; R1 after identity growth (24→28 layers) 0.990 (≈R0, preserved); R2 after the new top layers train on an LM objective (hidden drifts) 0.281; R3 after a cheap memory re-sync (retrain only the small modules on the evolved Qwen, NO backprop through Qwen) 1.000 (fully recovered). Cheap because new layers are at the END → backprop only through the top trainable layers + head. ENGINEERING GOTCHA: must load the model in **fp32** to train the grown layers — direct fp16 training (no master weights/loss-scaling) NaN'd the new layers and broke the representation irrecoverably (R2=R3=0). With fp32 it's clean. So the integrated "learn AND grow" loop holds on a real LM, not just the toy. The small→large thesis is now demonstrated end-to-end on both proxy AND Qwen.
- **AT-SCALE lifelong + growth on Qwen (`qwen_lifelong.py`)** — 192 distinct facts (6 sessions × 32) in a persistent Qwen memory bank. Before growth: recall ~0.94 FLAT across all 6 sessions (S0..S5: 0.97/1.00/0.94/0.91/0.84/0.97) → no forgetting by age, at scale, on real Qwen. After identity growth: IDENTICAL (growth preserves the whole 192-fact memory). After training the new core layers: drifts to ~0.4. After re-sync (train_mem 1200): partial recovery to ~0.63 — retrieval over 192 needs MORE re-sync steps than the small-scale (which fully recovered to 1.0 with 1000 steps over fewer facts). Fixes that made it work: bank must be DISTINCT (name,attr) (6× sample(32) collided) + train the memory at B≥96 so soft-attention retrieval is sharp over the 192-bank (B=48 → recall ~0.03). So: no-forgetting-at-scale + growth-preserves-all hold cleanly on Qwen; full re-sync recovery at scale just needs more steps. 25 commits; the small→large thesis is now end-to-end on proxy AND real Qwen, including at 192-fact scale.
- **PRE-CLOUD local validation (before renting a GPU).** On the RTX 2070/8GB: (1) all 24 modules import cleanly; (2) Qwen2.5-**1.5B** (28 layers, hidden 1536) loads in ~3.1GB fp16 → **frozen-feature scripts fit at 1.5B locally** (only fp32 training scripts need cloud); (3) `grow_qwen` is function-preserving at 1.5B (28→32, max|Δlogits|=0.0); (4) versioning-vs-RAG WIN holds at 1.5B (memory 0.98/1.00 vs RAG 0.31/0.14, sharper than 0.5B). FIXED a real scale bug: `qwen_integrated`/`qwen_lifelong` hardcoded `layers[24:]` (0.5B base depth) → now `n_base=len(layers)`. Added `QWEN_MODEL` env-var to all qwen_* scripts (pick size without editing). Cloud handoff files: `CLOUD_SETUP.md` (Tier-1 plan) + `requirements.txt` + `docs/memory/` snapshot in the repo. REMAINING local check (open): full re-sync recovery at scale on 0.5B (192-fact lifelong only recovered to 0.63 — needs more re-sync steps to confirm full recovery).
- **RE-SYNC-AT-SCALE OPEN QUESTION CLOSED.** `qwen_lifelong` re-sync bumped 1200→3500 steps: after identity growth (preserved) then core-drift (192-fact recall ~0.88→~0.44), the cheap re-sync now FULLY recovers to ~0.98 (S0..S5: 0.97/0.97/1.00/1.00/1.00/0.97). So the partial 0.63 earlier was just too few re-sync steps — full recovery at 192-fact scale confirmed. Local pre-cloud validation is now COMPLETE: every mechanism question answered on the 2070/0.5B(+1.5B frozen). Ready to move to a Tier-1 cloud GPU (see CLOUD_SETUP.md) for 1.5–3B training + growth.
- **DATA/COMPUTE-BUDGET → OPTIMAL-DEPTH, local validation (`diag_budget_depth.py`).** K-hop kmax=7, from-scratch sweep depth×budget, 2 seeds. Result: at BOTH budgets (1500, 6000 steps) the best depth is L*=4, and deeper (L6/L8) is WORSE (undertrained: B=6000 → L4 0.67 vs L8 0.37). So the MIS-SIZING PENALTY is real and large (growing too much for your budget hurts — the reason a data→params controller is needed) ✓. But L* did NOT shift deeper across this 4× budget range (both peak L4) — the scaling-law "more budget → deeper optimum" is a log effect needing ~10-100× budget spread. Follow-up: bigger budget (24000) to try to shift L*. So: the controller's JOB (avoid mis-sizing) is validated locally; the full L*(budget) curve needs a wider budget range (still local-feasible, just longer runs).
- **BUDGET→DEPTH follow-up (bigger budget) — validates the TWO-SIGNAL controller.** Same sweep at B=[6000,24000], depths [4,6,8]: L4 0.97→1.00 (task ceiling), L6 0.53→**0.94** (deeper CATCHES UP as budget 4×'s → "more data fills bigger capacity" confirmed), L8 0.36→0.40 (still undertrained / erratic single-seed). L* stayed 4 because kmax=7 is SOLVED by L4 (no headroom for deeper to WIN). KEY INSIGHT: budget alone doesn't set the growth amount — the controller needs BOTH (a) budget/data and (b) task saturation/headroom: if current depth already solves the task, don't grow regardless of budget. So the local env DID validate the data→params control question: mis-sizing penalty is real, more data pays off bigger capacity, and the two-signal (budget + saturation) controller design is empirically motivated. Remaining for cloud: does the relation hold at large model scale + a full neural/meta-learned controller. (diag_budget_depth.py kept at the canonical [1500,6000]/2-seed config.)
- **TWO-SIGNAL SEMI-NEURAL GROWTH CONTROLLER — HONEST NEGATIVE result (`diag_controller.py`, `diag_controller2.py`, 2026-07-01).** Built a controller that during ONLINE training senses 3 signals per 1000-step chunk — (a) loss-saturation (relative improvement <5%), (b) accuracy-headroom (<0.90), (c) remaining budget — and fires function-preserving `grow_deeper(2)`, budget-gated on amount. (1) On the ORIGINAL K-hop task (`diag_controller.py`): controller did **0 grows** at budgets 4000/12000 — because that task has a SHALLOW SHORTCUT (answer = unique source-never-target node = a set op L2 solves; L2 reached 0.985 at 12k), so headroom closed before saturation fired. Correct abstention, but growth never exercised. (2) Removed the shortcut with MULTI-CHAIN pointer-walk (`diag_controller2.py`: disjoint functional chains, query walks KWALK hops to its chain's tail, multiple sinks → must traverse). Now the controller DOES fire — but the naive single-chunk loss-delta is a **BAD saturation signal**: loss briefly plateaus right before a phase transition, so the controller mistakes temporary plateaus for saturation and grows PREMATURELY and repeatedly (grew 3× at 4k/9k/13k → L8); each grow adds identity layers that must re-warm, burning budget so nothing converges. Measured @budget 18000: controller (grew to L8) **0.243** vs **fixed-L8-from-scratch 0.482** vs fixed-L2 0.155 (fixed-L2 varies 0.16–0.47 across runs → toy task UNSTABLE). So the controller LOST to plain from-scratch. Earlier belief "deep underfits, growth rescues it" was a BUDGET artifact (at 6k L4/L6-from-scratch=0.16, but at 18k L8-from-scratch trains fine=0.48). Warm staged L2→L4→L6 (6k each) = 0.466→0.489→0.505 vs fixed-L2@18k 0.472 → growth pays only +0.03; and NO clean toy "Goldilocks" task exists (shortcut→shallow suffices; no-shortcut→~0.5 ceiling for everyone incl L6). TAKEAWAYS: (a) plateau-triggered growth needs a ROBUST saturation signal — patience / held-out validation slope / the meta-learned Ω (§27-28) — NOT a single-window loss delta; (b) toy scale (d=128, few-k steps, ~0.5 ceilings, high variance) CANNOT validate the capability payoff of growth-timing → this genuinely needs the real-model/cloud tier. The controller MECHANISM/wiring executes; its POLICY (when/how-much to grow) is the open research point, deferred to cloud + §28 meta-training.
- **MULTI-SEED AUDIT of "growth adds capability" — claim SURVIVES (sign-robust) but is BIMODAL (`diag_grow_hops_ms.py`, N=5, 2026-07-01).** Motivated by the controller work exposing high toy variance, re-ran the load-bearing capability claim across 5 seeds with 4 arms: A=L2 6k, B=L4 grown-from-A +6k (warm-start), C=L2 12k (2× compute, depth-vs-compute control), D=L4 from-scratch 12k (params/warm-start control). Result: **B(grown) − C(L2 2×-compute) is POSITIVE in 5/5 seeds at K4 and 4/5 at K5** → "growth adds DEPTH-capability, not just compute" is directionally robust to variance. BUT grown-L4 is **BIMODAL/lottery**: 3/5 seeds stay ~0.30 (gap only +0.04-0.09), 2/5 seeds BREAK THROUGH to ~1.00 (gap +0.77) — so the earlier SINGLE-SEED magnitude was not representative (toy is a coin-flip on whether the deep model "gets it"). KEY POSITIVE: **growth is the ONLY arm that ever cracks high-K** — grown hits K4/K5≈1.0 on the lucky seeds, but L4-FROM-SCRATCH (D) NEVER breaks through (max K4 0.41 across all 5 seeds) → warm-start growth isn't merely ≥ from-scratch, it's the sole route to the high-K breakthrough (means: B K4 0.59 vs D 0.33, C 0.39, A 0.25). CONCLUSION: the capability half of small→large is NOT a lucky-seed artifact in SIGN, and warm-start-growth > deep-from-scratch is the reliable takeaway; the MAGNITUDE is lottery-like at toy scale (d=128, 2→4L) → reliability (turning the 2/5 breakthrough into ~5/5) is exactly a real-scale question, not a local-tuning one. This audit de-risks the cloud spend: the mechanism is real, only its reliability needs scale.
- **CURRICULUM CANNOT LIFT THE GROWTH LOTTERY — reliability is a SCALE, not tuning, question (`diag_grow_hops_curric.py`, N=5, 2026-07-01).** Follow-up to the bimodal finding: tried to raise the 2/5 breakthrough rate with a post-growth K-CURRICULUM (after L2→L4 growth, ramp kmax 3→4→5 over the same 6k budget instead of uniform kmax=5), deepcopying the grown core so uniform vs curriculum start identically. Result: **NO difference** — breakthrough uniform 2/5 vs curric 2/5 (the SAME seeds 3,4 break through under BOTH; seeds 0,1,2 fail under BOTH), K3/K4/K5 means ~identical (0.80/0.65/0.54 vs 0.78/0.63/0.53). CONCLUSION: whether grown-L4 breaks through is set by the SEED (warm-start init / optimisation basin), NOT by post-growth training ORDER — so local training-recipe tuning can't fix the lottery. Combined with the earlier compute controls (L2-2× and L4-scratch also don't rescue stuck seeds), reliability of the growth-capability breakthrough is genuinely an init/scale/architecture question, not a local-tuning one. This CLOSES the "try to lift it locally" option and cleanly justifies moving to the cloud tier for reliability (multi-restart / larger width / real depth). Local mechanism-validation phase is now comprehensively complete, including the honest reliability boundary.

## CLOUD phase (RunPod RTX 5090 32GB, Blackwell/bf16, torch 2.8+cu128, 2026-07-01 night)
- **Port validated on real 5090**: `s0.run --smoke` fully green (capsule 1.000, versioning 1.000, commit-gate 1.000/0.000, lifelong 8 sessions flat 1.00 vs seq-LoRA ~0.02). bf16 compute works. Repo at pod /workspace/cl; results rsynced to `docs/cloud_results/`.
- **EXPERIMENT A — growth reliability vs WIDTH: naive width-scaling does NOT fix the lottery (honest negative) (`diag_grow_hops_scale.py`, N=8, budget 12k, lr 3e-3).** Breakthrough rate (K4&K5≥0.8) by d_model: **128→3/8, 256→0/8, 512→0/8**, and grown meanK4/K5 FELL with width (0.69/0.53 → 0.46/0.33 → 0.25/0.19; d=512 seed0 collapsed to 0.01 = divergence). So wider-at-fixed-budget/lr UNDERFITS (lr 3e-3 tuned for d=128 is wrong for 512; wider needs more steps + lower lr) → the "reliability is a free width-scale effect" hypothesis is REFUTED. HONEST caveat: this conflates width with under-compute + untuned-lr, so it does NOT prove scale can't help — it proves naive width-scaling hurts. KEY POSITIVE that SURVIVES: at EVERY width, **grown is the ONLY arm that ever breaks through** (grown 3/8 at d=128; L2x2 and L4scr both 0/8 at ALL widths) → the depth-via-growth advantage holds in SIGN; from-scratch and 2×-compute-L2 never crack high-K. Reliability fix needs compute+lr scaling (or better warm-start/basin), not just parameters.
- **EXPERIMENT B — growth-adds-capability on a REAL model, param-matched: IN-PLACE BEATS GROWTH (honest negative, thesis-sharpening) (`qwen_growcap.py`, Qwen2.5-0.5B, fp32, 4 trainable decoder layers, 6000 steps).** In-context multi-hop over single-token names; EXACTLY param-matched (both arms train 4 decoder layers = 59.6M, only DEPTH differs). Results (hop1/2/3): **BASE frozen 0.56/0.06/0.09 → INPLACE (unfreeze top 4 existing) 0.98/0.98/0.94 → GROW (train 4 appended identity) 0.96/0.92/0.87**. GROW−INPLACE gap = **−0.03/−0.05/−0.07 (GROW LOSES at every hop)**. So on an already-deep (24-layer) pretrained LM, adapting existing top layers beats growing new identity layers at matched params → **"growth adds capability in-place can't" is REFUTED on the real model** (exactly the pre-registered caveat: +4 layers is a small relative depth increase, and identity-init layers start functionally from scratch while in-place layers start from pretrained weights = better basin). IMPLICATION (sharpens the whole thesis): growth's value is FUNCTION-PRESERVATION / no-forgetting / continual addition WITHOUT disturbing old weights — NOT raw capability-per-param on a deep model. The "small→large via growth ADDS capability" story is toy-scale-specific (genuinely shallow→deep, and even there a lottery); it does NOT transfer to a real deep LM, where in-place ≥ growth for capability. Both trained arms crush BASE (0.09→0.87-0.94 @h3), confirming the task is depth-sensitive and the added capacity is what's learned.
- **EXPERIMENT C — ROBUST growth trigger FIXES the naive controller (clean POSITIVE) (`diag_controller3.py`, K-hop kmax=7, budget 8000, N=3).** Replaced the failed single-window training-loss delta with a HELD-OUT accuracy slope + PATIENCE (grow only after ≥2 consecutive chunks of no held-out gain — a real plateau, not a one-chunk dip — then cool down while new layers warm). Result: the controller grows **exactly ONCE (L2→L4) in all 3 seeds**, lands at L=4, and does NOT over-grow to L8 (the naive trigger grew 3× to L8 and lost). Mean acc: **controller 0.759 BEATS fixed L2 0.567, L4 0.568, L8 0.449** — without being told the depth it auto-lands at the sweet spot and beats capacity-limited L2 AND undertrained L8. Per seed (ctrl vs fixed-L4-from-scratch): seed0 0.929 vs 0.350 (warm-start growth hugely more reliable than L4-from-scratch), seed1 0.979 vs 0.998 (tie), seed2 0.368 vs 0.354 (hard seed, both fail). So warm-start growth to L4 > L4-from-scratch on the reliability axis too. CONCLUSION: today's earlier controller NEGATIVE was a SIGNAL-QUALITY problem, now fixed — a held-out-slope+patience trigger yields a working autonomous grow-to-sweet-spot controller that beats all fixed baselines. Caveats: N=3, seed2 unsolved by anyone (~0.36, variance persists), toy K-hop; the meta-learned Ω (§28) is the next step up from this hand-built-but-robust signal. This closes the loop opened by diag_controller2's failure.
- **EXPERIMENT A2 — A's width-fall was UNDERTRAINING; scale helps, but growth's depth-advantage VANISHES at scale (`diag_grow_hops_scale2.py`, N=6, compute+lr scaled to width).** Re-ran A with BUDGET and LR scaled to width (d128:12k/3e-3, d256:24k/1.5e-3, d512:40k/1e-3), arms grown vs L2ctrl(2×-compute). Breakthrough rate (K4&K5≥0.8): **d128 grown 2/6 / L2ctrl 0/6 (mean .70/.51); d256 grown 4/6 / L2ctrl 4/6 (mean .98/.89); d512 grown 4/6 / L2ctrl 4/6 (mean .94/.83)**. TWO honest points: (1) grown breakthrough RECOVERS vs A's fixed-budget collapse (d256 0/8→4/6, d512 0/8→4/6) → A's width-degradation was UNDERTRAINING/lr-mismatch, NOT a scale verdict; scale DOES make growth more reliable once properly trained. (2) BUT growth's UNIQUE advantage over a 2×-compute wide-shallow L2 (present at d=128: 2/6 vs 0/6) DISAPPEARS at d≥256 (grown 4/6 == L2ctrl 4/6) → given enough width AND compute, a shallow model catches up (width substitutes for depth). This converges with B (in-place ≥ growth on a real deep LM): growth does NOT buy unique capability at scale; a wide-enough / in-place-trained model matches it. COHERENT CROSS-EXPERIMENT CONCLUSION (A/A2/B): growth's value is NOT capability-per-param — it's the CONTINUAL / no-forgetting / function-preserving property (add capacity without touching or retraining old weights); for raw capability, wide-shallow-with-compute or in-place-fine-tune ties or beats growth. C separately shows the autonomous grow-to-sweet-spot CONTROLLER works with a robust (held-out+patience) signal.
- **EXPERIMENT C robustness at N=8 (`diag_controller3.py` C3_SEEDS=8) — the positive HOLDS but N=3 was optimistic.** Mean over 8 seeds: **controller 0.676 still beats fixed L2 0.613 / L4 0.475 / L8 0.480** (core claim survives). Behaviour sensible: grows to L4 on 6/8 seeds (1 grow each), correctly ABSTAINS (stays L2) on 2/8 where L2 already suffices (seed7 L2-stay 0.934, fixed-L2 0.977), and NEVER over-grows to L8. HONEST correction: the margin over fixed-L2 shrank from N=3's +0.19 (0.759 vs 0.567) to **+0.06** (0.676 vs 0.613), and per-seed the grow decision isn't always optimal (seed3 grew to L4 acc 0.646 but fixed-L2 was 0.970 there; seed6 fixed-L8 0.751 beat controller 0.405) — the K-hop task is high-variance (seed2 ~0.36 unsolved by all). So the robust held-out+patience trigger yields a controller that reliably beats fixed baselines on average, grows to the sweet spot or abstains appropriately, and never catastrophically over-grows — but the effect size is modest, not the large N=3 win. Reinforces tonight's meta-lesson: always multi-seed; single/few-seed magnitudes mislead. The controller MECHANISM + robust signal are validated; a meta-learned Ω (§28) is the path to sharper, per-seed-optimal grow decisions.
- **CAPSTONE — no-forgetting proven on real Qwen; growth/memory RETAIN, in-place FORGETS (`qwen_capstone.py`, Qwen2.5-0.5B, RunPod 5090, 2026-07-02).** The decisive test of the repositioned thesis ("grow to keep learning WITHOUT forgetting"). K=4 sessions x 24 DISTINCT facts learned ONE SESSION AT A TIME; after all 4, recall each session. Three carriers on the SAME stream, A and C training identical per-session params (top 2 decoder layers) / steps: **in-place(A)** unfreeze+keep-training top 2 shared layers, **memory(B)** external capsule (frozen base), **growth+routing(C)** per-session copy of top 2 layers + oracle route. FINAL per-session recall (S0=oldest): **in-place 0.17/0.17/0.29/1.00 mean 0.41** (CATASTROPHIC FORGETTING — each new session overwrites the shared weights, only the newest S3 survives, S0 crashed 1.00→0.17); **growth+routing 1.00/1.00/1.00/1.00 mean 1.00** (perfect retention by isolation); **memory 1.00/1.00/1.00/0.96 mean 0.99** (retention with NO routing, NO growth). CONCLUSION: on a real LM, growth's (and memory's) real value = CONTINUAL / NO-FORGETTING, exactly as last night's A/A2/B analysis repositioned it — shared-weight editing forgets catastrophically, isolating capacity (growth+routing) or externalising (memory) retains. HONEST asymmetry (printed): growth+routing needs an oracle session router AND grows 2 layers/session (unbounded); memory needs neither (external self-routed bank) → memory is the more elegant no-forgetting carrier, growth is the "add capacity per phase" carrier when you want it in-weights. This CLOSES the thesis loop: growth is not for capability-per-param (B/A2), it is for lifelong accumulation without forgetting (capstone), and an autonomous robust controller exists (C/diag_controller3). Caveats: single seed, 4x24 facts, oracle routing for arm C, in-context single-token facts.
- **CAPSTONE-2 — router-free no-forgetting holds MULTI-SEED at longer horizon (`qwen_capstone2.py`, Qwen2.5-0.5B, RunPod 5090, N=3 seeds, K=6 sessions x 16 facts).** Removes qwen_capstone's two caveats (single seed; oracle routing). Mean over 3 seeds, per session (S0=oldest): **in-place 0.23/0.12/0.21/0.29/0.50/1.00 (mean 0.39, S0 0.23)** — catastrophic forgetting with a clear AGE GRADIENT (older session = more forgotten, only newest S5 intact); **memory 0.98/0.98/1.00/1.00/1.00/1.00 (mean 0.99, S0 0.98)** — flat retention across the whole 6-session lifetime with NO session-id router (retrieves by key over the full bank); **growth+routing 1.00 everywhere (oracle ceiling)**. CONCLUSION: the no-forgetting result is robust (multi-seed) and, crucially, ROUTER-FREE via memory — you do NOT need to know which session a query belongs to; the external keyed bank self-routes and retains ~0.98-1.00 while shared-weight in-place editing forgets catastrophically (0.23 on the oldest). This is the strongest, cleanest form of the repositioned thesis: lifelong accumulation without forgetting, on a real LM, no oracle. Growth is the in-weights isolated-capacity alternative (oracle-routed ceiling); memory is the elegant router-free carrier. Caveats: 0.5B, in-context single-token facts, K=6/96 facts (retrieval still easy at this bank size — scaling the bank + ANN is the next axis). Cloud automation now fully working (RunPod API auto-provision: community 5090, 50GB disk, PUBLIC_KEY env -> direct TCP; ~10min image pull; pod uses `python` not `python3`).
- **EXPERIMENT A — memory bank SCALING: router-free RETRIEVAL scales, value-INJECTION training is unstable at large N (`qwen_memscale.py`, Qwen-0.5B, 5090).** Grew the bank 128→1200 DISTINCT facts (120 single-tok names × 10 attrs), precomputed frozen features once, trained the small memory modules on cached tensors. RESULT (retr@1 / top8 / ans:full / ans:top1 / ans:top8): 128→1.00/1.00/1.00/1.00/1.00; 512→0.97/1.00/0.97×3; 1024→0.96/1.00/0.97×3; 1200→**0.98/1.00 but ans ALL 0.032**. KEY FINDINGS: (1) **router-free retrieval@1 SCALES cleanly** — the learned keys stay discriminative (0.96–0.98) and the correct fact is ALWAYS in the top-8 (top8@ANN=1.00) up to 1200 → no session-id router needed even at 1200 facts. (2) **answer/injection recall is UNSTABLE at scale, not a clean law**: it held ~0.97 to N=1024 then collapsed to 0.032 at N=1200 — and crucially top1 (hard argmax injection) collapsed IDENTICALLY to full-softmax, so it is NOT softmax-blur (my initial hypothesis — DISPROVEN); the value path (val_enc/val_dec/gate) simply failed to converge for that run while the retrieval path was fine. Confirmed as VARIANCE: run-1 had N=512 collapse to 0.035 that run-2 (only diff = added eval, same training) recovered to 0.971. CONCLUSION: router-free memory retrieval scales to 1000+ facts robustly; the value-injection module training is variance-prone at large N (single-seed runs occasionally collapse) → reliable 1000+fact recall needs stabilised/multi-seed injection training (more steps / lower lr / better init), NOT ANN (ANN is speed-only; retrieval accuracy already scales). Honest correction of the "ANN needed for accuracy" guess. Next: stabilise+multi-seed the injection at N≥1024, and push bank size further with a bigger name pool.
- **EXPERIMENT A' (clean-up) — full-bank training makes router-free memory RELIABLE at scale (`qwen_memscale2.py`, Qwen-0.5B, 5090, 3 seeds).** Root cause of memscale's large-N collapse identified: TRAIN/EVAL MISMATCH — training retrieved/injected over only in-batch (128) keys while eval is over the whole bank, so at large N the eval softmax is far more diffuse than anything trained on. FIX: train retrieval AND injection over the FULL bank every step (cheap with cached frozen features). Result (mean over 3 seeds; retr@1 / ans:full / min-full-across-seeds): 512→1.00/1.00/1.00; 1024→0.99/0.67/**0.03**; 1500→0.99/**0.995**/**0.987**. So full-bank training makes N=1500 ROCK-SOLID across all 3 seeds (mean 0.995, min 0.987) where the old in-batch training collapsed at 1200 → **router-free 1000+fact recall is reliable** with the eval-matched objective. Residual: an OCCASIONAL single-seed collapse remains (1/3 seeds at N=1024, min 0.027; retrieval@1 still 0.99 → an injection-training fluke, now rare) — hardenable with more steps / restart-on-collapse, but the mechanism is validated. CONCLUSION for exp A: router-free memory retrieval AND recall both scale to ~1500 distinct facts on a real Qwen once training matches evaluation; ANN would only add speed (retrieval@1 ~0.99 already). Caveats: 0.5B, single-token facts, ≤1590 pairs (name-pool limit), rare residual instability.
- **EXPERIMENT A CLOSED — router-free memory reliably recalls up to 1500 facts, every seed (`qwen_memscale2.py` + restart-on-collapse, 5 seeds).** Added restart-on-collapse (post-train quick-check answer-recall < 0.5 → reinit small modules with a new seed + retrain, up to 3 tries) to kill the residual rare single-seed injection collapse. Result (mean over 5 seeds; retr@1 / ans:full / min-full / total-restarts): **512→1.000/1.000/1.000/1; 1024→0.998/0.999/0.996/0; 1500→0.989/0.991/0.971/4**. Every seed at every size now lands high (worst-seed full-recall ≥0.971). So collapses still occur occasionally (~1 per few seeds, more at 1500) but are auto-detected (quick-check cleanly separates collapsed ~0.03 from converged) and recovered by reinit. FINAL exp-A verdict: router-free capsule memory (no session-id) reliably retrieves AND recalls ~1500 distinct facts on a real Qwen-0.5B, given (a) full-bank-matched training and (b) restart-on-collapse; retrieval@1 ~0.99 throughout so ANN is speed-only. Experiment A (memory scaling) is complete and clean. Bank size ceiling here is the single-token name pool (159 names × 10 attrs = 1590); pushing to 10k+ needs a bigger entity space (and then ANN for speed). All cloud results in docs/cloud_results/.
- **SCALE ROBUSTNESS (exp B) — the no-forgetting contrast HOLDS at Qwen2.5-1.5B (`qwen_capstone2.py`, QWEN_MODEL=1.5B, seed 0 of 2; 5090).** Ran the router-free capstone at 1.5B to test whether the 0.5B result is size-specific. Seed 0 (K=6 sessions × 16 facts, per session S0=oldest): **in-place 0.31/0.38/0.25/0.12/0.38/1.00** (still CATASTROPHIC forgetting — only the newest S5 intact, mean ~0.41, essentially identical to 0.5B); **memory 1.00/1.00/1.00/0.94/0.94/0.94** (router-free retention ~0.97); **growth+routing 1.00 ×6** (perfect). So the pre-registered worry that a bigger model's top-layer fine-tune might interfere LESS (weakening the in-place-forgets contrast) did NOT materialise — in-place forgets just as badly at 1.5B, and memory/growth retain. The thesis is robust across 0.5B→1.5B. (Caveat: 1.5B seed 1 still running — capstone2's memory arm re-pools Qwen uncached so 1.5B is ~3× slower; seed 0 is decisive and matches the 3-seed 0.5B result. Next efficiency fix if scaling further: cache features in the capstone memory arm like memscale2 does.)
- **SCALE ROBUSTNESS (exp B) CORRECTION — full 2-seed 1.5B result (honest).** Seed 1 finished and CHANGES the picture from the seed-0-only note above. 2-seed means at Qwen-1.5B (K=6, S0=oldest): **in-place 0.38/0.28/0.25/0.25/0.50/1.00 (mean 0.44)** — robustly forgets both seeds (only newest intact); **growth+routing 1.00 ×6 (mean 1.00)** — robustly PERFECT both seeds; **memory mean 0.49** — but this is seed0 ~0.97 (retained) AVERAGED WITH seed1 ~0.01 (COLLAPSED to 0.00-0.06). The seed-1 memory collapse is the SAME value-injection training instability found in memscale — and capstone2's memory arm does NOT have the restart-on-collapse guard (that was only added to memscale2). So the honest 1.5B verdict: (1) in-place catastrophic forgetting is robust at scale; (2) growth+routing perfect retention is robust at scale (both seeds 1.00); (3) MEMORY retains at scale WHEN its injection training converges, but the injection-training instability persists/appears at 1.5B and needs the restart-on-collapse fix (from memscale2) to be reliable — without the guard it collapsed on 1 of 2 seeds. Correction of the over-optimistic seed-0-only note. TODO to fully close B: port restart-on-collapse into capstone2's memory arm and re-run 1.5B; the mechanism (memscale2) already shows it recovers cleanly. Growth's no-forgetting is the most scale-robust of the three carriers.
- **EXPERIMENT B CLOSED — no-forgetting thesis is scale-robust at Qwen-1.5B, all carriers clean (`qwen_capstone2.py` w/ cached+full-bank+restart memory arm, 2 seeds).** Ported the memscale2 fixes (cache features once, train retrieval+injection over the FULL bank, restart-on-collapse) into capstone2's memory arm — this both SPED UP the 1.5B memory arm (was uncached ~3× slow) and fixed the seed-1 collapse. Clean 2-seed means @1.5B (K=6, S0=oldest): **in-place 0.28/0.12/0.22/0.28/0.53/1.00 (mean 0.41)** — robustly catastrophic forgetting; **memory 1.00 ×6 (mean 1.00)** — router-free PERFECT retention BOTH seeds (seed 1 recovered from ~0.01 to 1.00 via the fix); **growth+routing 1.00 ×6 (mean 1.00)** — robustly perfect. FINAL exp-B verdict: the "grow/externalise to keep learning WITHOUT forgetting" thesis is ROBUST from 0.5B→1.5B — in-place fine-tuning forgets catastrophically at both scales (my worry that a bigger model would interfere less did NOT happen), while router-free memory and growth+routing both retain perfectly. The memory arm's earlier scale-instability is fully resolved by the eval-matched full-bank training + restart-on-collapse (same fix that closed exp A). Together A+B: router-free memory is reliable both in bank-size (to ~1500 facts, exp A) and in model-scale (0.5B & 1.5B, exp B). Caveats: 1.5B not 3B+, 2 seeds at 1.5B, K=6/96 facts per run.
- **EXPERIMENT D — pushing memory to 10k-scale reveals the VALUE-READOUT (not retrieval/bank-size) bottleneck (`qwen_memscale_big.py`, Qwen-0.5B, 3090Ti).** To exceed exp A's ~1500 (single-token-name limit), used a first×last name space (4347 names × 10 attrs = 43470 possible facts; only the ANSWER value needs single-token, keys can be multi-token). At N=1000: **retrieval@1 0.99, top32 1.00** (retrieval and ANN-shortlist scale perfectly to the large space) BUT **answer-recall only 0.06 (full-softmax) → 0.37 (top-32 injection)**, systematic across seeds (restart-on-collapse exhausted, min 0.09 — NOT a fluke). DIAGNOSIS: the collapse is in the VALUE path, not retrieval — memscale2 got recall 0.999 at N=1024 with SINGLE-token names, but here multi-token entity names make the mean-pooled frozen-feature value readout dilute the answer signal (longer statement "Alice Smith's favorite color is blue." → mean-pool spreads the answer token's contribution). So: (1) router-free RETRIEVAL scales to a large (10k+) fact space cleanly (retrieval@1 ~0.99); (2) the mean-pool value ENCODER/READOUT degrades for longer/multi-token facts, independent of bank size — this is the real bottleneck at realistic lifelong scale, and top-k(ANN) injection only partially helps. FIX (clean next step): a better value readout than mean-pool — e.g., last-token / answer-position hidden for the value encode, or a learned value pooler — which should restore recall for multi-token facts. Honest: exp D did NOT cleanly prove "recall scales to 10k" because reaching 10k required multi-token keys that exposed the readout limit; the retrieval half scales, the value-readout half needs a fix. (Compute note: 3090Ti is slow; restart-on-collapse wastes time when the collapse is systematic — gate restarts on a smarter signal.)
- **EXPERIMENT D CLOSED — router-free memory scales to ~10k facts with answer-position value readout (`qwen_memscale_big.py`).** The mean-pool value bottleneck (recall 0.06 at N=1000 with multi-token names) is FIXED by encoding the value from the ANSWER-POSITION last-token hidden of "X's attr is V" (no name-length dilution) instead of mean-pooling the whole statement. Result (mean/2 seeds; retr@1 / recall / top32): **1000→0.83/0.78/1.00; 4000→0.97/0.975/1.00; 10000→0.97/0.80/1.00**. So recall now TRACKS retrieval (was 0.06-0.37 with mean-pool → now 0.78-0.98), and router-free memory RETRIEVES AND RECALLS ~10k distinct facts (a real lifetime), with top32=1.00 everywhere (an ANN index preserves accuracy at speed). Residual: some variance at 10k (recall 0.80, min 0.65, 3 restarts) — more steps/seeds would tighten it, but the scaling is demonstrated. FINAL exp-D verdict: memory scales to 10k+ facts once (a) retrieval uses learned keys (already scaled), (b) the value uses answer-position readout (not mean-pool), (c) ANN/top-k readout for speed. Key lesson: at realistic lifelong scale the binding constraint was the VALUE ENCODER (mean-pool dilutes the answer for multi-token entities), not retrieval or bank size — and it has a clean fix. Combined A+B+D: router-free memory is reliable across bank-size (to 10k, exp A+D) AND model-scale (0.5B/1.5B, exp B). All cloud logs in docs/cloud_results/.
- **EXPERIMENT C CLOSED — GROWTH's no-forgetting is ROUTER-FREE too (`qwen_growroute.py`), closing the last oracle crutch.** The capstone's growth arm needed an oracle session-id; here the session is inferred by a TRAINED retrieval router (proj_k/proj_q InfoNCE on cached frozen features — the same mechanism as memory): store each fact's key labelled by session, a query routes to its nearest key's session → apply that branch. Findings (mean/2 seeds, K=4): **in-place 0.490 (forgets), growth-oracle 1.000 (ceiling), growth-ROUTED 0.990 (router-free) at routing accuracy 0.990.** DISCOVERY→FIX arc (same as A/B/D): a training-free RAW-pooled-key NN router routed at CHANCE (0.27 ≈ 1/K; same-attr keys too similar), and a lightly-trained router (2500 steps) hit only 0.55 → recall tracked it (0.62); a properly-trained router (6000 steps, seeded, full-bank InfoNCE) reached 0.99 routing → routed retention 0.99 ≈ oracle. So growth's no-forgetting is router-free once you route with a TRAINED retrieval router (not raw features), and retention is GATED by routing accuracy (routed ≈ oracle × route-acc). CONCLUSION: BOTH no-forgetting carriers are now router-free — memory (retrieve value by key) and growth (retrieve BRANCH by key) — using the same trained key-retrieval, which we've shown scales (retrieval@1 ~0.97 to 10k, exp A/D). The oracle-routing asterisk from the capstone is closed. Caveats: K=4 sessions, 0.5B, routing over 96-fact bank (routing at larger session counts needs the scaled retrieval from exp D). All four axes now closed: no-forgetting (capstone), bank-scale to 10k (A/D), model-scale 0.5B/1.5B (B), router-free for BOTH carriers (C).
- **ALL-NEURAL CONSTRAINT — honest status (2026-07-02): NOT yet fully neural.** The SUBSTRATE is neural/learned (key-query retriever proj_k/proj_q+InfoNCE, value enc/dec, injection gate, commit/admission gate, versioning ctx_enc, grown branch layers, frozen Qwen feats; routing uses learned similarities). But the META-CONTROL is still HAND-CODED heuristics: growth controller when/how-much (held-out-slope+patience thresholds in diag_controller3), restart-on-collapse (quick-check<0.5), readout/selection choices (top-k=32, answer-position last-token, discrete argmax→branch swap), and hyperparameters (LTOP=2, grow-2-layers, temps, lr, curriculum). The capstone's growth-ORACLE column used a hand-given session-id (replaced by the trained router in exp C). So "all-neural modulation" is the OPEN piece = §28 meta-learned Ω (learn when/how-much/where to grow + how to route/read out over simulated lifelong streams). Documented in FINDINGS.md ("Neural vs heuristic" section).
- **§28 ATTACK IN PROGRESS — meta-learned growth controller Ω built + correct, meta-RL is compute-bound (`diag_omega.py`, 2026-07-02).** Built Ω = small MLP mapping per-chunk observables (loss, rel-improvement, held-out acc, acc-delta, budget-used, depth, cooldown) → P(grow), meta-trained by REINFORCE across a K-hop task distribution (kmax varied) + budgets, reward = final-acc − λ·grows, tested on HELD-OUT kmax vs the hand heuristic (diag_controller3) and fixed depths. This neuralizes the biggest remaining heuristic (the growth decision). ENGINEERING REALITY: meta-RL = "train many networks", inherently heavy — full 300-ep run was ~10 min/episode (proxy training, not gen, is the cost; gen was fixed via batch-pool caching) → ~50 hr, infeasible on a shared community GPU. Shrunk to CHUNK=250 / budgets 1000-1500 / B=32 / 120 episodes (~1 min/episode → ~2 hr) for a preliminary run — IN PROGRESS at time of writing. So the §28 mechanism (learned grow policy + REINFORCE + held-out generalization test) is implemented and runs correctly (ep 0 produces sensible output); a CONVERGED Ω that beats the heuristic on held-out tasks needs the run to finish (and ideally a faster/dedicated GPU or a cheaper surrogate that predicts training curves without full training — the real way to scale meta-RL over training loops). Honest status: §28 is no longer just "open" — the controller is coded and training; the remaining gap is compute to convergence + the generalization result.
- **§28 FIRST-CUT meta-RL — honest NEGATIVE: Ω degenerated to "never grow" (`diag_omega.py` v1, 120 eps).** Held-out (greedy Ω / heuristic / fixed): kmax5 → Ω 0.24 (stayed L~2.0) vs heur 0.32 (L4) vs fixed L4 0.39; kmax8 → Ω 0.25 vs heur 0.26 vs L4 0.27. So the meta-learned controller LOST to both the hand heuristic AND fixed-L4 — it learned to never grow (acc = the L2 baseline). CAUSE: (a) 120 episodes is few for REINFORCE; (b) the tractability-shrink (budget 1000-1500 steps × B=32) REMOVED the regime where growth pays — with so few samples the grown layers can't train, so growing doesn't raise reward, and RL (correctly for that regime) learns "don't grow", plus the λ per-grow cost pushes the same way. KEY DIAGNOSTIC (answers "can we speed up"): GPU was only **13% util / 684MB / 126W** — the RL is kernel-launch/CPU-bound at tiny batch, NOT GPU-bound. FIX = big batch: same step count but 8-16× more samples/episode (barely more wall-time on the idle GPU) restores the grow-pays regime AND affords more episodes. So §28 v1 is an honest negative (heuristic still wins), with a clear compute-efficient path to a fair retry. The controller mechanism is validated; a USEFUL learned policy is not yet demonstrated.
- **§28 v2 — meta-learned Ω NOW LEARNS A SENSIBLE GROW POLICY (degeneracy fixed), matches the heuristic, doesn't yet beat it (`diag_omega.py` v2: B=512, CHUNK=150, budgets 600/900, 200 eps).** The v1 "never-grow" degeneracy was fixed by (a) bigger batch (GPU 13%→66%, restores the grow-pays sample regime) and (b) removing per-step CPU-GPU syncs + shrinking CHUNK to keep grow-decision points with fewer total steps. Held-out result (greedy Ω / heuristic / fixed L2/L4/L6): **kmax5 → Ω 0.81 (L~6) / heur 0.84 / 0.69 / 0.90 / 0.92; kmax8 → Ω 0.73 (L~6) / heur 0.78 / 0.58 / 0.46 / 0.73.** VERDICT: the growth decision is now a WORKING LEARNED NEURAL POLICY — Ω grows appropriately (not degenerate), GENERALIZES to held-out kmax, matches the hand heuristic (0.81≈0.84, 0.73≈0.78), and beats fixed-L2/L4 (esp. kmax8 where from-scratch L4 collapses to 0.46 while Ω/L6=0.73). BUT Ω does NOT yet BEAT the heuristic, nor fixed-deep-from-start at kmax5 (L6 0.92 > Ω 0.81) — staged-growth controllers pay a cost vs training deep from the start when the task is solvable at known budget. So §28 first cut = the biggest heuristic (growth trigger) is SUCCESSFULLY NEURALIZED and generalizes (advances the all-neural goal), but "learned > hand-crafted" is not yet shown; that needs richer reward shaping / obs / more episodes / better credit assignment for the staged-growth cost. GPU note: util plateaus ~66% for the tiny d=128 model (kernel-launch-latency-bound; removing syncs didn't raise it) — real wall-time lever is fewer steps (big batch gives ample samples), not chasing util.
- **INTEGRATION — one autonomous lifelong LOOP composes the validated pieces (`diag_system.py`, proxy, local).** Assembled memory + growable core + trained retriever + growth controller into a SINGLE running system over a lifetime stream: each phase ingests 32 new facts (→ capsule memory) and runs a capability phase (train the K-hop core; controller grows on plateau); recall of ALL past facts + capability measured throughout. Result over 6 phases: facts 32→192, depth 2→6 (2 growth events), capability(K6) 0.33→0.47, final recall 0.88 over 192 facts. So ONE loop accumulates knowledge AND grows capability autonomously — the pieces COMPOSE into a running system (the "assemble the car" milestone). HONEST caveats: (1) recall transiently DROPS when the core drifts (capability training moves the features the memory rides on) and RECOVERS via a cheap per-phase re-sync — the known core-evolution↔memory tension; it takes a phase to stabilise (phase0 end 0.03 → later phases 0.86-0.93), and the FIX that made it work was caching frozen features per re-sync + always re-syncing after every core change (not only on growth). (2) capability gains are modest (0.33→0.47; K6 is hard at this budget) — consistent with "growth adds capability slowly". (3) the write-vs-grow decision here is still rule-based (facts→memory, plateau→grow); making THAT choice neural is the next integration step. This is the first end-to-end system demonstration; still synthetic/0.5B-proxy, single run.
- **WRITE-vs-GROW decision NEURALIZED (`diag_writegrow.py`) — the last integration-layer hand-rule is now learned.** A gate (MLP over the frozen-core last-token feature of an incoming item) decides WRITE-to-memory vs CONSOLIDATE-via-growth, trained as a contextual bandit (reward by (type,action): fact+write=1.0, fact+grow=0.05, skill+grow=0.85, skill+write=0.0). Held-out result: **routing accuracy 1.00 (fact→write 1.00, skill→grow 1.00), mean reward 0.925 = ORACLE 0.925, vs always-write 0.50 / always-grow 0.45.** So the gate learns the correct write-vs-grow policy FROM FEATURES ALONE (not told the type), matching the oracle and crushing single-mechanism baselines. Combined with §28 (neural growth trigger) and the trained retriever (neural routing), the integrated loop's modulation is now neural at every decision point: what-to-store (admission gate §27.18), write-vs-grow (this), when/how-much-to-grow (§28 Ω), and route/recall (trained retriever). CAVEAT: fact-vs-skill is cleanly separable from features so the gate learns it easily; the harder genuine COST-TRADEOFF case (same item could go either way; decision depends on system state / memory load / frequency) is not yet tested — that's the meaningful next hardening. First cut: the decision is demonstrably neuralizable.
- **WRITE-vs-GROW hardened to a GENUINE COST-TRADEOFF — gate learns a STATE-DEPENDENT policy (`diag_writegrow2.py`).** Removed the easy fact-vs-skill type signal: every item is a fact, and the right action depends on system STATE (write value = freq·(1−load/CAP) degrades as memory fills; consolidate value = freq·0.95 − C, reliable but costs compute). A gate over [freq, load] trained as a contextual bandit: **matches the oracle action 0.81 (89/110), mean reward 0.404 ≈ oracle 0.417 (97%), beats fixed-heuristic 0.380 / always-consolidate 0.372 / always-write 0.275.** DECISIVE: the learned consolidate-threshold on frequency DROPS with load (load 0.1→0.91, 0.5→0.63, 0.9→0.36) — i.e. the gate consolidates MORE when memory is loaded, exactly the optimal state-dependent behaviour, NOT a fixed rule. So the write-vs-grow decision is neural even in the genuine cost-tradeoff regime (not just the separable type case). Minor: 0.81 (not 1.0) action-match is misclassification near the indifference boundary where reward impact is tiny (hence reward still 97% of oracle). This closes the write-vs-grow hardening flagged in diag_writegrow.

## grow-to-large branch (2026-07-02)
- **"成大模型 = grow to become MORE CAPABLE" — HONEST NEGATIVE (`diag_growlarge.py`, branch grow-to-large, 3 seeds).** Tested the literal headline: a small model grown through an ESCALATING K-hop curriculum (kmax 3→7, L2→L10) vs fixed-small(L2) vs fixed-large(L10 from scratch), same total budget, accuracy by K at the end. Result (mean/K, 3 seeds): **grown→L10 mean 0.47, fixed-small L2 mean 0.54, fixed-large-scratch mean 0.68**; high-K (K≥5) grown 0.22 ≈ small 0.23. So STAGED GROWTH IS WORSE than fixed-small (each grow adds identity layers that must re-warm → wastes the per-stage budget) AND far worse than training large-from-scratch. Growth gave ZERO high-K capability benefit. WHY: the curriculum is a CUMULATIVE single task (same relation, rising kmax) → there are no DISTINCT skills to forget → growth's only real advantage (no-forgetting) doesn't apply, and its capability advantage doesn't exist (consistent with earlier growth≠capability / bimodal-lottery / width-substitutes findings). CONCLUSION: the naive "grow small→large to become more capable at a task" is REFUTED — if you want a large capable model for one task, train it large from scratch; growing is strictly worse. The supportable small→large story is the OTHER one: grow to RETAIN accumulated DISTINCT skills/knowledge without forgetting (capstone), where "large" = capacity for retention, not per-task capability. This sharply bounds the vision.
- **"成大模型 = grow to COVER many skills" — NULL (`diag_growlarge2.py`, branch, 3 seeds).** Complement to the capability negative: N=5 distinct reasoning SKILLS (K-hop over 5 different relations), grown system (frozen base + per-skill routed branch) vs fixed-small trained sequentially. Result: **grown 0.78 ≈ fixed-small 0.77, BOTH master 5/5 skills** — NO growth advantage. WHY: K-hop-over-relations is ONE GENERALIZABLE skill (the model learns to follow whatever relation the query names), so the fixed-small model generalizes across relations and does NOT forget → growth's no-forgetting advantage doesn't apply. COMBINED with diag_growlarge (grow-to-be-more-capable = negative): the "成大模型" vision does NOT hold for reasoning capability (per-task IQ, refuted) NOR for generalizable reasoning skills (parity). Growth's small→large value is SPECIFICALLY for CONFLICTING knowledge — distinct FACTS that overwrite each other in shared weights (the capstone: in-place forgets facts 0.41, memory/growth retain 1.00). So the honest, tightly-bounded reading of "小模型永續學習成大模型": grow to hold ever more CONFLICTING FACTUAL knowledge without forgetting (large = capacity for retained conflicting facts), NOT to become a better reasoner. This branch delivered two honest negatives that precisely delimit the vision.
- **REVERSAL — "grow AND get smarter" IS achievable with the right CADENCE (`diag_growlarge3.py`, 3 seeds).** Deep re-think of the growlarge negative found the confound: it grew EVERY stage (5 grows × 1500 steps — exactly the premature/frequent-growth failure mode diag_controller2 already identified). Same curriculum/budget, cadences compared: **every-stage(L10) 0.47 < fixed-small(L2) 0.54 < once-mid(ONE grow L2→L6 before stage 3) 0.72 < fixed-L6-scratch 0.82** (mean over K1-7; at K7 once-mid 0.44 vs fixed-small 0.17). So ONE well-timed growth event with ample post-growth budget DOES make the model genuinely more capable (+0.18 over never growing) — the earlier "growth doesn't add capability on a curriculum" was a CADENCE artifact, consistent with all our controller findings (rare well-timed growth wins; frequent growth burns budget re-warming). HONEST boundary that stands: fixed-large-FROM-SCRATCH still beats grown (0.82 vs 0.72) when you KNOW the final size and can retrain from zero — but continual learning by definition cannot (unknown final size, accumulated state); grown reaches 88% of the from-scratch ceiling while starting small. Plus at real LLM scale the stacking literature reports growth is compute-CHEAPER than from-scratch, so the toy's from-scratch edge may not transfer. REFINED verdict for 成大模型: grow-to-be-smarter works with controller-timed rare growth (and the §28 Ω/heuristic controllers are exactly the mechanism to pick those moments); frequent naive growth is the failure mode. Remaining test in flight: distill-into-core COMPOSITION (diag_compose) — whether consolidating memory into grown weights unlocks cross-session 2-hop composition that single-shot retrieval cannot do.
- **COMPOSITION via distill-into-core — honest NEGATIVE at toy scale (`diag_compose.py`).** Tested the §27.12 "smarter" mechanism: r1:X→Y (session1), r2:Y→Z (session2), train atomic facts + a subset of compositions into a GROWN core, evaluate HELD-OUT compositions ("the r2 of the r1 of X"). Result (2 seeds): all arms behave exactly as mechanism predicts on the easy parts — memory single-shot: 1-hop 1.00 / 2-hop 0.00 (can't chain); fixed-small sequential: forgets f1 → comp 0; distill+grown: knows ALL atomic facts (1.00/1.00), memorizes SHOWN compositions (1.00) — but **held-out composition = 0.00-0.06: NO compositional generalization**. A grokking-length retry (8× steps = 30k, 80% shown) ALSO 0.00. So latent in-weights 2-hop does NOT emerge at toy scale (d=128, NX=60) even with extended training — consistent with the known "latent two-hop / compositionality gap" literature (grokked implicit reasoning needs very specific conditions/scale). The distill→compose "smarter" mechanism remains THEORETICALLY the right target but is NOT demonstrated; it needs grokking-scale compute or larger models. FINAL synthesis on 「長大也變聰明」: (1) YES via CADENCE — one controller-timed grow genuinely adds capability (+0.18 over fixed-small, growlarge3); (2) NOT YET via distill→composition (this negative); (3) real-scale stacking compute-efficiency untested (literature-positive). The supportable full story: grow rarely & well-timed to gain capability, grow/externalize to retain conflicting facts, and composition-smartness awaits scale.
- **CLOSED — the AUTONOMOUS controller realises "grow AND get smarter" (`diag_autocap.py`, 3 seeds).** Composed the robust growth controller (held-out capability plateau + patience + budget/cooldown gating, the diag_controller3 signal) with the escalating K-hop curriculum: it decides WHEN to grow by itself. Result: **autonomous mean 0.77** vs reference (growlarge3) grow-every 0.47 / fixed-small 0.54 / hand-tuned once-mid 0.72 / L6-scratch 0.82. So the autonomous controller BEATS fixed-small (+0.23), beats grow-every (+0.30), and EXCEEDS even the hand-tuned once-mid (0.77 vs 0.72) — it found a capability-adding cadence on its own (grows 3-4× at the right moments as the curriculum escalates, reaching depth 8-10). Per seed: seed1 excellent (K7 0.97 at depth 8), seed0 good (K7 0.70), seed2 collapsed (K7 0.15 — grew but the deep model didn't converge, the persistent growth-lottery variance on hard seeds). HONEST: mean 0.77 with high variance (2/3 seeds strong, 1 poor); not bulletproof, but the mechanism works. This closes the "grow-to-be-smarter, autonomously controlled" thread: the neural/robust controller autonomously picks the growth timing that makes a small model genuinely more capable, without a hand-set schedule. Full corrected 成大模型: sparse controller-timed growth adds capability (autocap 0.77) + growth/memory retain conflicting facts (capstone) + composition-smartness awaits scale (compose negative).
- **ROBUSTIFIED — autonomous grow-and-get-smarter, variance fixed via KEEP-BEST checkpoint (`diag_autocap2.py`, 3 seeds).** autocap reached mean 0.77 but a seed collapsed to 0.15 (grew to L10, deep model failed to optimise). First tried REVERT-on-regression: it killed the collapse (worst 0.15→0.50) but the short post-grow verdict window (3 chunks) reverted BENEFICIAL-but-slow growths too → shallow models → mean dropped to 0.67. Better fix = KEEP-BEST checkpoint: track the model state with the highest reference capability (kmax6) seen and return THAT. Result: **mean 0.84, worst-seed 0.55** (vs autocap 0.77/0.15) — mean UP and the collapse GONE. The seed that collapsed to 0.15 in autocap now returns 0.99 (its pre-collapse peak); seed1 0.98; only seed0 weak (0.55, a genuinely hard seed whose best-ref was only 0.62). Mean 0.84 now EXCEEDS even fixed-large-from-scratch (0.82) and hand-tuned once-mid (0.72). So the autonomous, robust controller makes a small model grow AND get smarter reliably: keep-best guarantees a bad growth never tanks the run. Honest: one hard seed still ~0.55 (variance reduced, not eliminated). This is the strongest, cleanest form of 'grow to be smarter' — autonomous timing (controller) + robustness (keep-best) + the corrected cadence insight.
- **A — grow-cadence-smarter TRANSFERS to real Qwen (`qwen_growcap_curric.py`, Qwen-0.5B).** Faithful Qwen version of the toy autocap/growlarge3 cadence result: frozen Qwen + APPENDED trainable decoder layers, escalating multi-hop curriculum (hop1→3), three cadences at matched budget. Accuracy by hop (mean): **fixed-small(+2) 0.85, grown(2→4) 0.89, fixed-large(+4) 0.98**; grown ≥ fixed-small at EVERY hop (hop2 0.85 vs 0.76, hop3 0.77 vs 0.73). So the corrected conclusion HOLDS on a real pretrained model: ONE well-timed grow (2→4 layers mid-curriculum) makes Qwen more capable than staying small — the "grow AND get smarter with the right cadence" result transfers. Margin is MODEST (+0.04 mean, vs toy's +0.18) because pretrained Qwen isn't capacity-bound the way toy-L2 is; and fixed-large-FROM-SCRATCH still wins (0.98, exactly as in toy — from-scratch-at-final-size beats growing when you know the size and can retrain, which continual learning can't). Caveats: single seed, small margin, hop1 eval had a harmless count artifact (fixed post-hoc; all arms shared the same eval so the comparison stands). NET: the whole 成大模型 corrected story now has a real-model data point — grow-timed-well beats fixed-small on Qwen, direction-consistent with the toy.

## Autonomous rounds (2026-07-03)
- **ROUND 1 — A CLARIFICATION (honest DOWN-ADJUST): grow-cadence-smarter does NOT robustly transfer to real Qwen (`qwen_growcap_curric.py`, 3 seeds).** Multi-seeded the single-seed +0.04 from before. Result: grown-minus-fixed-small margin **mean +0.003, positive in only 2/3 seeds [+0.028, +0.005, −0.025]**; mean-by-hop grown 0.76 == fixed-small 0.76 (fixed-large 0.79). So the earlier single-seed +0.04 was WITHIN NOISE — on a pretrained Qwen-0.5B, adding 2 trainable layers is NOT reliably better than training the existing 2 (Qwen isn't capacity-bound like toy-L2). CORRECTS the prior over-optimistic "transfers to Qwen" claim. HONEST BOUND: grow-for-capability holds robustly only at TOY scale where the base is genuinely capacity-limited (autocap 0.84); it washes out on a pretrained deep model with a few trainable top layers. The robust real-model value remains NO-FORGETTING (capstone) + memory scaling, not capability. (Community pod reclaimed the first 3090Ti run mid-way; reran on a 5090.)
- **ROUND 2 — WHY-GROW clarified: growth is the best FEASIBLE streaming strategy AND incremental growth beats deep-from-scratch (`diag_stream.py`, 3 seeds, matched compute).** Escalating K-hop stream, total budget matched. Mean-by-K: **grown 0.84 (feasible) > large-oracle 0.68 (NOT feasible: needs known final-size + full replay) > fixed-small 0.54 (feasible, capacity-bound) > retrain-from-scratch-each-stage 0.34 (feasible but wasteful — restarting burns compute).** TWO clarifications: (1) among strategies actually FEASIBLE in a stream (can't know final size, can't replay all data), grown wins decisively (0.84 vs 0.54 vs 0.34) — answering "why grow": it's the compute-efficient continual strategy. (2) STRONGER: grown (0.84) even BEATS the large-oracle (0.68) here because the oracle target (L10) is DEEP and deep-from-scratch UNDERFITS, while incremental warm-start growth optimizes better. So the earlier "from-scratch-at-final-size always wins" boundary only holds when the final size is SHALLOW enough to train from scratch (growlarge3 L6-scratch 0.82 won); at genuine DEPTH (L10), incremental growth wins on optimization too — exactly the real-LLM stacking-efficiency argument (growth cheaper/better than from-scratch as models get deep). Caveat: grown still bimodal (seed0 0.55, seed1 0.98; keep-best gives mean 0.84). This directly repairs the round-1 pessimism: growth's capability value is real in the setting that matters (streaming + deep targets), even if it washes out in the easy non-streaming shallow-Qwen probe.
- **ROUND 3 — DEPTH CROSSOVER confirmed: incremental growth beats from-scratch at DEEP targets (`diag_depthcross.py`, 3 seeds, kmax=7).** Fixed hard task, matched budget, grown-to-L (warm-start staged) vs scratch-L, for L in {4,6,8,10,12}. grown-minus-scratch: **L4 −0.11 (scratch wins, shallow), L6 +0.08, L8 +0.22, L10 +0.01, L12 +0.15 (grown wins, deep)** — crossover at L=6. So from-scratch-at-final-size wins ONLY for shallow targets (L≤4); at genuine DEPTH (L≥6) incremental warm-start growth is the better route to capability (deep-from-scratch underfits at matched budget). This PRECISELY BOUNDS the "from-scratch always wins" boundary from earlier and is the toy analogue of the real-LLM stacking-efficiency result. IMPLICATION for round-1's Qwen null: that probe used only 2→4 trainable top layers (SHALLOW, below the crossover), which is exactly the regime where growth has no edge — so the null is consistent with the crossover, and the honest test on Qwen should use DEEPER trainable stacks (round 4). Growth's capability value is real, specifically at depth.
- **ROUND 4 — composition via distill-into-core STILL does not emerge at d=512 (`diag_compose` CP_D=512, 2 seeds, 5k+15k steps).** Scaled the width 4× and training ~3×. Result: distill+grown core knows all atomic facts (f1 1.00, f2 1.00) and (seed1) memorizes SHOWN compositions (1.00), but **held-out 2-hop composition = 0.00 on both seeds** — identical to d=128. So cross-session 2-hop composition (the "grow+distill → compositionally smarter" mechanism) does NOT emerge at toy scale even at 4× width; it is a genuine latent-multi-hop / grokking-scale phenomenon needing real scale, confirmed. (Side note: the memory arm's retrieval also degraded at d=512 with the fixed 2500-step module training — a scale-of-training issue, not the point.) CONCLUSION: of the "grow to be smarter" mechanisms, DEPTH-via-well-timed-growth works (rounds 2/3: grown beats deep-from-scratch at L≥6, streaming-feasible), but COMPOSITION-via-distill does NOT at reachable toy scale — it awaits real scale. Honest bound stands.
- **ROUND 5 — depth crossover reproduces at d=256 (width-robust at the crossover point, but noisy) (`diag_depthcross.py` DC_D=256, 2 seeds, targets 4/6/8).** grown-minus-scratch: L4 −0.365 (scratch wins, shallow), **L6 +0.142 (grown wins — SAME crossover as d=128)**, L8 −0.181 (grown COLLAPSED to 0.005 — growth-lottery, amplified at d=256 with only 2 seeds and NO keep-best). So the L=6 crossover (grown beats deep-from-scratch) is WIDTH-ROBUST at the crossover point, confirming round 3 at 2× width; but the grown arm is noisier at bigger width/depth (needs keep-best + more seeds for a clean curve — cf. autocap2 where keep-best fixed exactly this collapse). PROCESS NOTE: tried running d=256 and d=512 in PARALLEL on one GPU — backfired (compute-bound deep-model jobs contend, ~½ throughput each); reverted to single-job. Lesson: parallel helps only launch-bound small jobs, not compute-bound ones. Directional confirmation of the depth-crossover's width-robustness; the growth variance is the same lottery keep-best addresses.
- **ROUND 6 — CLEAN definitive crossover with keep-best: incremental growth beats from-scratch at ALL depths (`diag_depthcross.py` + keep-best in grown_to, d=128, 3 seeds).** Added keep-best (track the best-capability checkpoint along the L2→L growth trajectory, return it) to kill the round-5 lottery collapse. Result grown-minus-scratch: **L4 +0.075, L6 +0.322, L8 +0.287** — grown wins at EVERY target depth (margin grows with depth), no collapses. This is the DEFINITIVE version: keep-best incremental warm-start growth dominates from-scratch training at all depths (crossover effectively at L4). Interpretation: "grown-to-L with keep-best" = the best checkpoint on the growth trajectory, a robust strategy ≥ any single-depth from-scratch. Cleanly closes the grow-capability axis (rounds 2-6): growth-for-capability is real and robust when you (a) grow incrementally with well-timed cadence, (b) keep the best checkpoint; from-scratch only competes at shallow targets and only without keep-best. Combined with the honest bound (Qwen frozen-shallow probe was null, round 1; composition needs scale, round 4), the corrected 成大模型: incremental keep-best growth genuinely adds capability at depth, in the streaming regime that continual learning actually faces.
- **ROUND 8 — long-lifetime no-forgetting CONFIRMED at K=10 sessions on Qwen (`qwen_capstone2` CAP_K=10, 2 seeds).** Extended the load-bearing no-forgetting result from K=4-6 to a LONGER lifetime. Mean over 2 seeds (S0=oldest of 10): **in-place 0.12/0.22/0.22/0.19/0.28/0.34/0.38/0.50/0.56/1.00 (mean 0.38)** — catastrophic forgetting with a CLEAN MONOTONIC AGE GRADIENT (oldest S0 down to 0.12, each older session more forgotten, only newest S9 intact); **memory (router-free) 1.00 ×10 (mean 1.00, flat)**; **growth 1.00 ×10**. So router-free memory retains ALL 10 sessions perfectly over a longer lifetime, while in-place forgetting gets WORSE with lifetime length (K=4 oldest 0.17→0.23, K=10 oldest 0.12) — the longer you live, the more shared-weight editing forgets, but memory stays flat. This confirms the "永續 (perpetual) learning without forgetting" claim at a longer horizon, multi-seed, on the real model. The load-bearing no-forgetting result is robust across lifetime length (K), model scale (0.5B/1.5B), and bank size (10k facts).
- **ROUND 9 — integrated full-system demo (honest, half-clean) (`diag_fullsystem.py`, 3 seeds).** A DECOMPOSED system (external capsule memory for facts on frozen features + a keep-best growing core for capability) vs a MONOLITH (one L2 core, facts+capability in shared weights) over a 5-phase lifelong stream (accumulating facts + escalating K-hop). Mean/3 seeds: **decomposed fact-recall 0.94 + capability 0.82; monolith fact-recall 0.96 + capability 0.40.** So decomposed wins decisively on CAPABILITY (0.82 vs 0.40 — growth breaks the L2 capacity ceiling) and TIES on fact-recall. HONEST nuance: the monolith did NOT forget facts here because I gave it REPLAY (re-samples all past facts every phase); the no-forgetting advantage only appears WITHOUT replay (streaming), which the capstone already showed cleanly (in-place-no-replay forgets to 0.12 at K=10). So this demo cleanly shows the CAPABILITY half of the decomposed advantage (memory+growth matches facts AND beats monolith on capability); the no-forgetting half is the capstone's (streaming/no-replay). COMBINED, the decomposed architecture is robust to BOTH failure modes that break a monolith — streaming/no-replay (→ external memory retains) AND capacity limits (→ keep-best growth adds capability) — while a small monolith is capped on capability even when replay saves its facts. The corrected vision, integrated: external memory + controller-timed keep-best growth.
- **ROUND 10 — memory-scaling LIMIT found (honest bound on "scales to 10k"): learned retrieval COLLAPSES at 20k+ (`qwen_memscale_big` MB_SIZES=8k/20k/40k, MB_STEPS=6000).** Result (retr@1 / recall / top32): **8000 → 0.73/0.73/0.98; 20000 → 0.00/0.02/0.00 (restarts exhausted); 40000 → 0.00/0.02/0.00.** So the router-free LEARNED-KEY retrieval has a real scaling limit at this compute: clean to ~1.5k (0.99), DEGRADED and high-variance by ~8-10k (this run 8k=0.73 vs round-D 10k=0.97 — variance), and TOTALLY COLLAPSED at 20k+ (0.00, 4 restarts all fail) — the 20k-way InfoNCE retrieval simply doesn't converge with 6000 steps / 256-dim keys. This CORRECTS the round-D "scales to 10k" claim: 10k was near the EDGE (variance-prone), and 20k+ is beyond reach of the learned-key approach at this budget. To go to 20k-1M facts you need much more retrieval training / larger key dim / hierarchical retrieval, OR — likely better — ANN over FROZEN features with NO learned retrieval-training (the training is the bottleneck, not the frozen features). Honest bound: router-free memory is reliable to ~a few thousand, edge at ~10k, and needs a different (non-learned-InfoNCE or hierarchical) retrieval beyond ~20k. The no-forgetting mechanism holds; its scale ceiling with THIS retrieval is ~10k.
